{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Task Index Schema",
  "description": "Schema for claude-code-go task index file",
  "type": "object",
  "required": ["tasks"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "lastUpdated": {
      "type": "string",
      "format": "date"
    },
    "description": {
      "type": "string"
    },
    "tasks": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["id", "title", "priority", "status", "file"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "priority": { "type": "string", "enum": ["P0", "P1", "P2"] },
          "estimatedTime": { "type": "string" },
          "dependencies": {
            "type": "array",
            "items": { "type": "string" }
          },
          "status": { "type": "string", "enum": ["pending", "in_progress", "completed", "blocked", "skipped"] },
          "file": { "type": "string" },
          "record": { "type": "string" }
        }
      }
    },
    "statusEnum": {
      "type": "array",
      "items": { "type": "string" }
    },
    "priorityEnum": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
