{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Task Index Schema",
  "description": "Schema for task index file",
  "type": "object",
  "required": ["feature", "created", "status", "tasks"],
  "properties": {
    "feature": {
      "type": "string"
    },
    "prd": {
      "type": "string"
    },
    "design": {
      "type": "string"
    },
    "proposal": {
      "type": "string",
      "description": "Path to proposal document (quick mode alternative to prd/design)"
    },
    "created": {
      "type": "string",
      "format": "date"
    },
    "status": {
      "type": "string",
      "enum": ["planning", "in-progress", "completed"]
    },
    "statusEnum": {
      "type": "array",
      "items": { "type": "string" }
    },
    "priorityEnum": {
      "type": "array",
      "items": { "type": "string" }
    },
    "testCommand": {
      "type": "string",
      "description": "Shell command for running project-wide tests (overrides auto-detection)"
    },
    "e2eRound": {
      "type": "integer",
      "minimum": 0,
      "maximum": 3,
      "description": "Current fix-e2e round counter. 0 = no failures yet. Incremented each time e2e tests fail and fix tasks are appended. Max 3 rounds."
    },
    "tasks": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["id", "title", "priority", "status", "file"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "priority": { "type": "string", "enum": ["P0", "P1", "P2"] },
          "estimatedTime": { "type": "string" },
          "dependencies": {
            "type": "array",
            "items": { "type": "string" }
          },
          "status": { "type": "string", "enum": ["pending", "in_progress", "completed", "blocked", "skipped", "rejected"] },
          "file": { "type": "string" },
          "record": { "type": "string" },
          "breaking": {
            "type": "boolean",
            "description": "When true, task completion triggers full test suite across all layers",
            "default": false
          },
          "scope": {
            "type": "string",
            "enum": ["frontend", "backend", "all"],
            "default": "all",
            "description": "Task scope: must be explicitly set by breakdown-tasks using the Scope Assignment algorithm. 'all' for cross-layer, summary, gate, and T-test tasks; 'frontend'/'backend' for single-layer tasks in mixed projects."
          },
          "mainSession": {
            "type": "boolean",
            "description": "When true, run-tasks executes this task in the main session instead of dispatching to task-executor. Used by tasks that need to spawn subagents.",
            "default": false
          },
          "type": {
            "type": "string",
            "enum": [
              "implementation",
              "doc-generation.summary",
              "doc-generation.consolidate",
              "test-pipeline.gen-cases",
              "test-pipeline.eval-cases",
              "test-pipeline.gen-scripts",
              "test-pipeline.run",
              "test-pipeline.graduate",
              "test-pipeline.verify-regression",
              "fix",
              "gate"
            ],
            "description": "Task type. Determines executor behavior: noTest tasks (doc-generation.*, gate, test-pipeline.*) skip quality gate; mainSession tasks (gate) run in main session."
          },
          "blockedReason": {
            "type": "string",
            "description": "Human-readable reason why this task is blocked. Set when status is 'blocked'."
          },
          "noTest": {
            "type": "boolean",
            "description": "Deprecated: use type field instead. When true, this task does not require tests (e.g. documentation-only, summary tasks). Skips quality gate and test evidence checks. Coverage is auto-set to -1.0.",
            "default": false
          }
        }
      }
    }
  }
}
