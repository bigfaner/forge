---
id: "T-test-2"
title: "Generate e2e Test Scripts"
priority: "P1"
estimated_time: "1-2h"
dependencies: ["T-test-1"]
status: pending
---

# Generate e2e Test Scripts

## Description

Call `/gen-test-scripts` skill to generate executable TypeScript e2e test scripts from test cases.

Generated scripts use:
- UI tests: Playwright Locator API (semantic locators)
- API tests: Node.js built-in `fetch`
- CLI tests: `child_process.execSync`
- Test framework: `@playwright/test` (test runner with built-in timeouts, fixture injection)

## Reference Files

- `testing/test-cases.md` — Test case document (generated by T-test-1)
- `docs/sitemap/sitemap.json` — Page element locators (required for UI tests, generated by `/gen-sitemap`)
- `tests/e2e/config.yaml` — Test environment config (generated by `/gen-test-scripts` or manually created)

## Acceptance Criteria

- [ ] `tests/e2e/features/<slug>/` contains at least one spec file (ui.spec.ts / api.spec.ts / cli.spec.ts)
- [ ] `tests/e2e/helpers.ts` exists (shared infrastructure)
- [ ] Each test() includes traceability comment `// Traceability: TC-NNN → {PRD Source}`

## User Stories

No direct user story mapping. This is a standard test generation task.

## Implementation Notes

1. Run `/gen-test-scripts` skill
2. Verify spec files exist under `tests/e2e/features/<slug>/`
3. If T-test-1 was skipped, mark this task as skipped as well
4. After generating spec files, run `just e2e-verify --feature <slug>`; if exit 1 (unresolved `// VERIFY:` markers), task is incomplete — resolve markers before proceeding to T-test-3
5. After generating spec files, run TypeScript compilation check: `cd tests/e2e && npx tsc --noEmit`
6. If compilation fails, fix the generated spec files before marking the task complete
