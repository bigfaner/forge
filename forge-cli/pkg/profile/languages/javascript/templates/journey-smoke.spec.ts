describe("@feature", () => {
  it("Journey smoke test - happy path", async () => {
    const { execSync } = require("child_process");

    // Step N: <action>
    // const stepN = execSync("<binary> <args>").toString();
    // expect(stepN).toMatch(/<pattern from Fact Table>/);
  });
});
