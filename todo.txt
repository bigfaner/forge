1、增加一个系统化修复Bug的slash command或skill，先复现问题，补充单元测试和e2e测试，再修复问题，最后验证。done
2、优化独立agent占用token的问题
3、完善动态追加任务的机制：例如e2e测试失败，追加修复问题的任务。done
4、遵循harness enginnering思想，持续迭代forge插件
5、拆分forge，把辅助性的skill或slash command迁移到其它插件
6、优化生成的feature e2e测试脚本的质量。done
7、增加一个改进forge本身的skill或其它能力
8、优化gen-sitemap的输出结果，避免重复的节点
9、增加一个eval-test-cases skill。done
10、task record命令检查验收标准是否完成？
11、调整eval-prd的评分比重，用户故事提到高30分。done.
12、改造tech-design skill：涉及到数据库表设计或变更时，需要输出到独立的E—R图文档和SQL文件，并必须得到人类的同意后，才可以继续。done
13、init-justfile通过参数指定语言、项目类型，适应项目初始时什么都没有的场景。done
14、ui-design默认生成原型。done
15、write-prd之后，先ui-design（如有UI），再tech-design。done
16、精简guide.md。done
17、eval-design的breakdown-tasks门槛提高到18分。done
18、增加一个快速模式，brainstorm —> breakdown-tasks —> run-tasks 。done
19、task claim输出breaking字段的值。done
20、补充一个git-checkout命令，创建一个与正在讨论的提案或feature名称相同的分支。done
21、all-completed stop hook：just lint 等出现错误，动态追加任务。done
22、增加一个开启subagent评估一致性的skill。done
23、有些任务只输出文档，没有代码，不需要执行测试，task record需要放过，避免报错。done

Bash(task record T-test-1b --data docs/features/train-recorder/tasks/process/record.json)
  ⎿  Error: Exit code 1                                                                                                                      
     ---                                                                                                                                     
     ERROR_CODE: VALIDATION_ERROR           
     ERROR: Cannot mark task completed with no test evidence                 
     CAUSE: testsPassed=0 and testsFailed=0 with status=completed suggests tests were not actually run
     HINT: Either (1) run tests and report results, (2) set coverage=-1.0 for tasks without tests, or (3) use --force to override            
     ACTION: task record <id> --data record.json  (with real test metrics)
     task record <id> --data record.json --force  (override, use cautiously)                                                                 
     ---  

24、eval-ui的评分必须达到94，才允许自动进入到生成原型的阶段。done
25、把git-commit skill更改为slash command。done
26、从pm-work-tracker、train-recorder提取e2e测试的经验，来优化forge的测试能力。doing
27、forensic默认调查当前会话，/status获取会话ID？要知道如何排查当前项目的会话-- /Z--project-ai-coding-train-recorder/
28、对比get-shit-done、superpowers、gstack等同类产品
29、init-justfile支持覆盖式更新，just run 等命令去掉 --dev参数，更改为skill，内置模板，但是禁止大模型调用。
30、forensic分析任务卡住不动时，要展示各个动作的耗时
31、task feature 要支持worktree
32、重构forge
  1. task cli 扩展为forge cli，forge task *，forge forensic *，forge prompt *
  2. 通过cli根据任务类型合成启动subagent的提示词。不同类型的任务采用不同的流程。
  3. 技术设计时选定e2e测试框架，给gen-test-scripts指明方向
  4. cli提取任务的formatter汇总成index.json
33、评估提案要增加需求或逻辑是否全面的维度，整体逻辑是否一致，合理性，可行性。done
34、forge的流程是否太重。
35、开启subagent深度评估一致性：@task-cli/pkg/prompt/data/ 中的模板，run-tasks skill、task-executor agent                                  
  等任务调度机制所有关联方。然后就问题逐个向我提问 
36、增加forge proposal 命令展示当前项目的所有提案，包括创建时间（以文件创建时间为准）、是否关联了prd、是否完成。done
37、增加forge feature [list|status] --<feature> 命令查询feature的状态，是否完成。doing
forge lesson，查看lesson
forge config，交互式，提供内置选项，让用户输出必要的信息，初始化.forge/config.yaml
forge init，初始化.gitignore和Claude.md、.forge/config.yaml，.gitignore要忽略forge的运行时文件。复制 CLAUDE.template.md到项目根目录下，并改名为Claude.md
38、增加forge kb 命令对接知识库，集中管理编程知识。
39、justfile拆分test：just test（快速，无-race）和 just test-race（完整检测，CI/发布前用）
40、forge，ui-design支持tui。done
41、forge，分多个任务生成测试用例。doing
42、forge cli 生成各阶段的gate和sumary任务。doone
43、末尾增加clean code的任务 — 使用 /simplify 替代，见 docs/proposals/reject-clean-code-task/
44、快速模式不限制任务数量，只是流程简化。done
45、record-task skill 更名为submit-task跟forge task submit命令对齐。done
46、优先使用justfile中的命令，而不是profile/manifest.yaml中的。done
47、把使用nodejs编写的测试用例转成go。done
48、forge cli根据任务类型决定是否追加测试任务，适配不同的场景。done
49、去掉forge claim等输出的冗余信息。done
50、forge init安装justfile。内置安装包，用户也可以自行安装。done
51、末尾增加调整文档内容，保持一致性的任务。
52、调整consolidate-spec任务，在feature结束时，自动维护`docs/conventions`与`docs/business-rules/`中的文档，使其与最新代码保持一致。doing
53、forge feature set <feature> ，写入到.forge/state.json。同时，task feature支持从.forge/state.json获取feature。要与worktree一起考虑。
54、forge cli增加一个分配测试用例编号的命令？避免冲突。
55、追加doc-score和doc-revise任务到index.json。待评估
56、T-eval-doc任务记录的内容，调整一下。