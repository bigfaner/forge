---
id: "2.2"
title: "Add Execution Workflow to quick templates + update schemas"
priority: "P0"
estimated_time: "2h"
dependencies: ["2.1"]
status: pending
breaking: true
noTest: false
mainSession: false
---

# 2.2: Add Execution Workflow to quick templates + update schemas

## Description

Add `## Execution Workflow` section to all 6 quick-tasks templates, following the same pattern established in task 2.1 for breakdown templates. Remove `noTest` from all frontmatter. Remove `noTest` field definition from both `index.schema.json` files.

## Reference Files

- `plugins/forge/skills/quick-tasks/templates/task.md`
- `plugins/forge/skills/quick-tasks/templates/quick-test-cases.md`
- `plugins/forge/skills/quick-tasks/templates/quick-gen-scripts.md`
- `plugins/forge/skills/quick-tasks/templates/quick-run-tests.md`
- `plugins/forge/skills/quick-tasks/templates/quick-graduate.md`
- `plugins/forge/skills/quick-tasks/templates/quick-verify-regression.md`
- `plugins/forge/skills/breakdown-tasks/templates/index.schema.json`
- `plugins/forge/skills/quick-tasks/templates/index.schema.json`
- `docs/features/task-executor-skeleton/tasks/records/2.1-breakdown-templates.md` — Phase 2.1 record (for workflow pattern reference)

## Acceptance Criteria

- [ ] All 6 quick templates have `## Execution Workflow` section with non-empty content
- [ ] All 6 quick templates have no `noTest` in frontmatter
- [ ] Both `index.schema.json` files have no `noTest` field definition
- [ ] `grep -r "noTest" plugins/forge/skills/quick-tasks/templates/` → zero matches (in .md files)
- [ ] `grep "noTest" plugins/forge/skills/breakdown-tasks/templates/index.schema.json` → zero matches
- [ ] `grep "noTest" plugins/forge/skills/quick-tasks/templates/index.schema.json` → zero matches

## User Stories

**Story 1** (Template Author):
- AC1: task-executor reads ## Execution Workflow → all 6 quick templates now declare their workflows

**Story 3** (Forge Maintainer):
- AC3: no noTest in template frontmatter → removed from all 6
- AC4: schema no noTest field, ajv validate passes → removed from both schemas

## Implementation Notes

- Quick templates mirror breakdown templates: `task.md` → same TDD workflow, `quick-test-cases.md` → mirrors gen-test-cases, etc.
- Reference task 2.1's record for the established workflow pattern — use the same style and structure
- For schemas: remove the entire `noTest` property block from `properties.tasks.additionalProperties.properties`
- After schema changes, verify JSON is valid (no trailing commas, etc.)
