---
id: "1.1"
title: "Rewrite task-executor.md as workflow skeleton"
priority: "P0"
estimated_time: "2h"
dependencies: []
status: pending
breaking: true
noTest: false
mainSession: false
---

# 1.1: Rewrite task-executor.md as workflow skeleton

## Description

Rewrite `agents/task-executor.md` Step 2 to be a pure workflow skeleton. Remove all hardcoded TDD logic (RED/GREEN/REFACTOR, Quality Gate). Add 3-case dispatch: found workflow → follow it, no workflow → read default template, empty workflow → warning + read default template. Renumber steps (6→5). Remove NO_TEST from Inputs section. Apply same changes to `commands/execute-task.md`.

## Reference Files

- `plugins/forge/agents/task-executor.md` — Agent prompt to rewrite
- `plugins/forge/commands/execute-task.md` — Single-task command (mirrors task-executor)
- `docs/features/task-executor-skeleton/design/tech-design.md` §Interface 1 — Skeleton Step 2 spec

## Acceptance Criteria

- [ ] Step 2 contains zero TDD-specific instructions (no RED/GREEN/REFACTOR keywords, no TDD cycle description)
- [ ] Step 2 has 3-case dispatch: Case A (found workflow), Case B (no workflow → read default template), Case C (empty → warning + fallback)
- [ ] Case B references default template at `plugins/forge/skills/breakdown-tasks/templates/task.md`
- [ ] NO_TEST removed from Inputs section
- [ ] Steps renumbered: old Steps 4→3, old Step 5→4 (total 5 steps: 0-4)
- [ ] `commands/execute-task.md` mirrors the same skeleton structure with no NO_TEST references
- [ ] grep -i "NO_TEST\|noTest" in both files → zero matches

## User Stories

**Story 1** (Template Author):
- AC1: task-executor reads ## Execution Workflow from task files → addressed by Step 2 Case A
- AC2: no workflow → TDD fallback → addressed by Step 2 Case B (reads default template)
- AC3: empty workflow → warning + fallback → addressed by Step 2 Case C

**Story 2** (Task-executor Agent):
- AC2: execution record shows "Execution Workflow" not "TDD implementation" → Step 2 output format specifies this
- AC3: workflow complete → Step 3 (Record), no Quality Gate → Step 2 no longer has QG

**Story 3** (Forge Maintainer):
- AC7: task-executor.md has no NO_TEST input → removed from Inputs section

**Story 4** (Execution Failure):
- AC1: file unreadable → failed → Step 1 behavior (unchanged)
- AC2-3: workflow failure handling → Step 2 output captures failure reason
- AC4: partial completion → failed → Step 2 output captures completed steps

## Implementation Notes

- The new Step 2 prompt text is specified verbatim in tech-design.md §Interface 1
- The old Step 2 (TDD) and Step 3 (Quality Gate) merge into the new Step 2
- The old Step 4 (Record) becomes new Step 3, old Step 5 (Commit) becomes new Step 4
- For execute-task.md: apply the same skeleton Step 2, same renumbering, same NO_TEST removal
- Do NOT add ## Execution Workflow to any template in this task — that's task 2.1/2.2
