---
id: "3.1"
title: "Remove NO_TEST from commands and skill docs"
priority: "P0"
estimated_time: "1.5h"
dependencies: ["1.summary"]
status: pending
breaking: false
noTest: false
mainSession: false
---

# 3.1: Remove NO_TEST from commands and skill docs

## Description

Remove all `NO_TEST` / `noTest` / `--no-test` references from commands (run-tasks.md, execute-task.md) and skill docs (record-task, quick-tasks, consolidate-specs). These are documentation/prompt changes that align the dispatcher and skill instructions with the new workflow architecture.

## Reference Files

- `plugins/forge/commands/run-tasks.md` — Dispatcher (remove NO_TEST from claim parsing + dispatch)
- `plugins/forge/commands/execute-task.md` — Single-task command (remove NO_TEST references, should already be done by 1.1 — verify)
- `plugins/forge/skills/record-task/SKILL.md` — Remove noTest references in quality gate pre-check section
- `plugins/forge/skills/quick-tasks/SKILL.md` — Remove `--no-test` flag and noTest references
- `plugins/forge/skills/consolidate-specs/SKILL.md` — Remove noTest reference in Step 9
- `docs/features/task-executor-skeleton/design/tech-design.md` §File Change Inventory — Skill docs and commands

## Acceptance Criteria

- [ ] `run-tasks.md`: no NO_TEST in claim parsing (lines ~51-59) or dispatch prompt (lines ~87-98)
- [ ] `execute-task.md`: no NO_TEST references (should be clean from task 1.1, but verify)
- [ ] `skills/record-task/SKILL.md`: no noTest references (coverage auto-set mention, quality gate skip mention, validation rule)
- [ ] `skills/quick-tasks/SKILL.md`: no `--no-test` flag, no noTest in frontmatter copy instructions
- [ ] `skills/consolidate-specs/SKILL.md`: no noTest reference in Step 9
- [ ] `grep -ri "noTest\|NO_TEST\|--no-test" plugins/forge/commands/run-tasks.md plugins/forge/commands/execute-task.md plugins/forge/skills/record-task/SKILL.md plugins/forge/skills/quick-tasks/SKILL.md plugins/forge/skills/consolidate-specs/SKILL.md` → zero matches

## User Stories

**Story 3** (Forge Maintainer):
- AC5: commands grep zero match → run-tasks.md and execute-task.md cleaned
- AC6: skills grep zero match → record-task, quick-tasks, consolidate-specs cleaned

## Implementation Notes

**run-tasks.md changes:**
- Remove NO_TEST from the claim output parsing section (alongside KEY, TASK_ID, etc.)
- Remove NO_TEST from the dispatch prompt template (`NO_TEST: {{NO_TEST}}`)
- The dispatch prompt should only pass: KEY, TASK_ID, FILE, BREAKING, MAIN_SESSION, SCOPE, FEATURE

**record-task/SKILL.md changes:**
- Remove references to `noTest: true` in coverage section
- Remove quality gate skip condition for noTest tasks
- Remove validation rule about noTest for "No test evidence" error
- Update suggestion: instead of "add noTest:true", suggest "--force"

**quick-tasks/SKILL.md changes:**
- Remove `--no-test` flag from Flags section
- Remove conditional "unless --no-test" from Step 4
- Remove noTest from index.json copy rules
- Remove noTest reference from Output Checklist

**consolidate-specs/SKILL.md changes:**
- Remove Step 9 reference to "Omit coverage from record.json -- the noTest flag auto-sets it"
- Update to simply say "Omit coverage from record.json" or remove the note
