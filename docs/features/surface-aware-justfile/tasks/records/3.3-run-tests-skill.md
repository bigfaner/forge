---
status: "completed"
started: "2026-05-26 01:23"
completed: "2026-05-26 01:27"
time_spent: "~4m"
---

# Task Record: 3.3 run-tests SKILL.md 调度器模式重写

## Summary
Rewrote run-tests SKILL.md to dispatcher mode: detect surface-type from task frontmatter or forge surfaces CLI -> load rules/surfaces/<type>.md -> orchestrate just recipe sequence. Removed test.execution config reading. Created 5 surface orchestration rule files (web, api, cli, tui, mobile). Implemented probe HARD-GATE and exit code semantics per BIZ-error-reporting-001.

## Changes

### Files Created
- plugins/forge/skills/run-tests/rules/surfaces/web.md
- plugins/forge/skills/run-tests/rules/surfaces/api.md
- plugins/forge/skills/run-tests/rules/surfaces/cli.md
- plugins/forge/skills/run-tests/rules/surfaces/tui.md
- plugins/forge/skills/run-tests/rules/surfaces/mobile.md

### Files Modified
- plugins/forge/skills/run-tests/SKILL.md

### Key Decisions
- Followed Interface-2 spec over AC for CLI/TUI sequence: test->teardown (not build->dev->test) since init-justfile rules define no dev/probe recipes for cli/tui
- Probe retry: max 3 attempts, 5-second interval per tech-design.md Interface-2
- Surface detection: two-source priority (frontmatter first, forge surfaces CLI fallback), exit 2 on both-failure
- Kept existing rules (env-check, result-parsing, confidence, failure-diagnosis, test-isolation) unchanged as they remain valid

## Test Results
- **Tests Executed**: No
- **Passed**: 0
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] web surface executes dev -> probe -> test -> teardown sequence
- [x] probe failure exit 1 (retryable) triggers teardown + exit 1
- [x] probe failure exit 2 (blocking) triggers teardown + exit 2
- [x] probe HARD-GATE: no retry or dev restart in same cycle
- [x] surface info unavailable: stderr error with recovery hint + exit 2
- [x] CLI/TUI surface executes test -> teardown (no probe/teardown per Interface-2)

## Notes
SPEC CONTRADICTION: AC says CLI/TUI executes 'build -> dev -> test (no probe/teardown)' but Interface-2 cli.md/tui.md only define test+teardown recipes. Followed Interface-2 as more specific directive. No Go code changes -- skill docs only. coverage=-1.0 signals no test runner invocation for markdown-only changes.
