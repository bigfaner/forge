---
status: "completed"
started: "2026-05-25 23:34"
completed: "2026-05-25 23:44"
time_spent: "~10m"
---

# Task Record: 1.1 MatchSurface API 增强 + forge surfaces --json

## Summary
Enhanced MatchSurface API to return SurfaceMatch{Key, Type} struct and added --json flag to forge surfaces CLI with three output modes (list, query, types)

## Changes

### Files Created
无

### Files Modified
- forge-cli/pkg/forgeconfig/match.go
- forge-cli/pkg/forgeconfig/surfaces_test.go
- forge-cli/internal/cmd/surfaces.go
- forge-cli/internal/cmd/surfaces_test.go

### Key Decisions
- SurfaceMatch struct defined in pkg/forgeconfig with JSON tags for direct serialization
- JSON query mode distinguishes no-match ([]exit 0) from no-config (JSON error exit 1) by checking surfaces emptiness before calling MatchSurface
- JSON error output uses json.NewEncoder(cmd.ErrOrStderr()).Encode() per Hard Rules, not fmt.Fprintf

## Test Results
- **Tests Executed**: Yes
- **Passed**: 35
- **Failed**: 0
- **Coverage**: 88.1%

## Acceptance Criteria
- [x] MatchSurface() returns SurfaceMatch{Key, Type} struct
- [x] forge surfaces path --json outputs [{"key": ..., "type": ...}]
- [x] forge surfaces --json (list mode) outputs {"surfaces": [...]}
- [x] forge surfaces --types --json outputs {"types": [...]}
- [x] Missing surfaces config with --json outputs JSON error to stderr exit 1
- [x] runSurfacesQuery adapted to new signature, text output unchanged without --json
- [x] go test ./pkg/forgeconfig/... ./internal/cmd/... all pass

## Notes
Scalar form returns Key='.' per spec. No-match path query in JSON mode returns empty array [] with exit 0, distinguishing from config-missing which returns JSON error with exit 1.
