---
id: "3.6"
title: "死代码清理"
priority: "P2"
estimated_time: "1h"
dependencies: ["3.5", "2.gate"]
scope: "all"
breaking: false
type: "coding.cleanup"
mainSession: false
---

# 3.6: 死代码清理

## Description

删除已被 surface 机制替代的死代码和过时文档：prompt.go 中的 `extractTestTypeArg()` 和 `genScriptBases()` 函数，run-test 的 `config-schema.md` 中 test.execution 文档。

## Reference Files

`design/tech-design.md#Phase-Component-Map` — Phase 3 死代码清理项

## Acceptance Criteria

- `extractTestTypeArg()` 已从 prompt.go 删除
- `genScriptBases()` 已删除
- run-tests `references/config-schema.md` 中 `test.execution` 节点文档已移除
- `go build ./...` 成功
- `go test ./pkg/prompt/...` 通过
- 无代码引用被删除的函数

## User Stories

No direct user story mapping.

## Implementation Notes

extractTestTypeArg() 在任务 1.4 中已被 task.SurfaceType 直接读取替代。genScriptBases() 被 surface 规则文件的配方生成机制替代。config-schema.md 的 test.execution 节点：残留配置被 Go YAML 宽松解析静默忽略，无需运行时迁移。
