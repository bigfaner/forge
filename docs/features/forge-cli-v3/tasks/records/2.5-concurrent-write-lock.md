---
status: "completed"
started: "2026-05-14 00:14"
completed: "2026-05-14 00:42"
time_spent: "~28m"
---

# Task Record: 2.5 Concurrent write conflict handling for submit

## Summary
Add advisory file locking to handle concurrent write conflicts on submit. Created pkg/index package with LockFile/UnlockFile using Windows LockFileEx (cross-platform via build constraints), 5-second timeout returning ErrLockConflict, and SaveIndexAtomic using temp-file + rename pattern. Integrated into submit.go to acquire lock before reading index.json and defer unlock. Lock files persist for reuse; cleanup does not touch .lock files.

## Changes

### Files Created
- forge-cli/pkg/index/lock.go
- forge-cli/pkg/index/lock_windows.go
- forge-cli/pkg/index/lock_unix.go
- forge-cli/pkg/index/atomic.go
- forge-cli/pkg/index/lock_test.go

### Files Modified
- forge-cli/internal/cmd/submit.go

### Key Decisions
- Used build constraints (lock_windows.go / lock_unix.go) for cross-platform file locking instead of external dependency
- Named import alias indexPkg to avoid collision with local variable 'idx' (renamed from 'index')
- Used defer+retErr pattern in SaveIndexAtomic for clean cleanup on error paths
- Lock files (index.json.lock) persist for reuse rather than being created/deleted each time

## Test Results
- **Tests Executed**: Yes
- **Passed**: 21
- **Failed**: 0
- **Coverage**: 80.3%

## Acceptance Criteria
- [x] pkg/index/lock.go created with LockFile and UnlockFile
- [x] ErrLockConflict error defined: concurrent write conflict, retry
- [x] Lock file: index.json.lock (created if not exists, reused)
- [x] 5-second timeout on lock acquisition returns ErrLockConflict
- [x] Windows support via LockFileEx syscall
- [x] submit.go acquires lock before reading index.json, defers unlock
- [x] Atomic index write: write to .tmp file then os.Rename
- [x] Error cases: lock timeout exit 1, lock creation failure exit 1
- [x] Unit tests: concurrent access, timeout, lock release
- [x] forge cleanup does NOT touch .lock files
- [x] go test ./... passes

## Notes
pkg/index coverage is 80.3%. The uncovered paths are I/O error handling (write/close/rename failures) which are difficult to trigger in tests without mocking filesystem. The concurrent access test and timeout test each take ~5 seconds due to real timeout behavior.
