---
status: "completed"
started: "2026-05-13 23:43"
completed: "2026-05-14 00:12"
time_spent: "~29m"
---

# Task Record: 2.4 Quality-gate max fix-task cap

## Summary
Added max fix-task cap (3 per step) to quality-gate. When 3 active fix-tasks exist for a step, addFixTask returns ErrMaxFixTasks instead of creating a new task. Added countActiveFixTasks function, maxFixTasksPerStep constant, and updated addFixTask signature from (string) to (string, error). Updated title format to 'fix <step>:' prefix convention for consistent fix-task identification.

## Changes

### Files Created
无

### Files Modified
- forge-cli/internal/cmd/quality_gate.go
- forge-cli/internal/cmd/quality_gate_test.go

### Key Decisions
- Changed addFixTask return type from string to (string, error) -- callers ignore the error via blank identifier since handleGateFailure already handles empty fixID gracefully
- Title format changed from 'Fix: <step> failure in quality gate' to 'fix <step>: <testScript> failure in quality gate' to enable prefix-based fix-task identification
- Cap check loads index at the start of addFixTask (already loaded later for AddTask), reusing the same indexPath variable
- If index cannot be loaded for cap check, proceed without cap (graceful degradation)

## Test Results
- **Tests Executed**: Yes
- **Passed**: 366
- **Failed**: 0
- **Coverage**: 80.8%

## Acceptance Criteria
- [x] countActiveFixTasks(index *task.TaskIndex, step string) int function added to quality_gate.go
- [x] maxFixTasksPerStep = 3 constant defined
- [x] addFixTask checks cap before creating fix-task: returns ('', ErrMaxFixTasks) when cap exceeded
- [x] Fix-task identification: SourceTaskID != '' AND title has prefix 'fix <step>:'
- [x] Terminal states (completed, skipped) excluded from count
- [x] When cap reached: stderr 'max fix-tasks reached for <step>, manual intervention required', exit code 1
- [x] Unit tests: 3 active fix-tasks -> cap error on 4th; completed/skipped fix-tasks not counted
- [x] go test ./... passes

## Notes
无
