---
status: "completed"
started: "2026-05-14 01:04"
completed: "2026-05-14 01:10"
time_spent: "~6m"
---

# Task Record: 3.1 pkg/e2e/ core: profile resolution + ExecRunner

## Summary
Created pkg/e2e/ package with RunOpts struct, ResolveProfile function (reads config.yaml via profile.ReadTestProfiles, validates against profile.IsKnownProfile), ExecRunner interface with RealExec production implementation, and stubExec hand-rolled mock for testing. Sentinel errors ErrNoProfile, ErrBadProfile, ErrFeatureNotFound defined with PRD-matching messages.

## Changes

### Files Created
- pkg/e2e/e2e.go
- pkg/e2e/exec.go
- pkg/e2e/e2e_test.go
- pkg/e2e/exec_test.go

### Files Modified
无

### Key Decisions
- Used fmt.Errorf with %w wrapping for ErrBadProfile so errors.Is works while producing PRD-matching message 'unknown profile: <value>'
- ResolveProfile returns first profile from config.yaml test-profiles list
- RealExec.Run uses CombinedOutput() for simplicity; stubExec uses map[string]execResponse pattern matching project convention
- Package-level runner var defaults to RealExec; tests override with stubExec
- ErrFeatureNotFound defined but not yet used (reserved for future task)

## Test Results
- **Tests Executed**: Yes
- **Passed**: 11
- **Failed**: 0
- **Coverage**: 81.8%

## Acceptance Criteria
- [x] pkg/e2e/e2e.go created with RunOpts struct, ResolveProfile(projectRoot string) (string, error)
- [x] pkg/e2e/exec.go created with ExecRunner interface, RealExec struct, package-level runner variable
- [x] ResolveProfile uses profile.ReadTestProfiles() for reading config.yaml
- [x] ResolveProfile validates against known profiles, returns ErrNoProfile or ErrBadProfile
- [x] Error messages match PRD spec: 'no e2e profile configured', 'unknown profile: <value>'
- [x] Unit tests with hand-rolled stubExec mock matching project convention (no testify/gomock)
- [x] Unit tests cover: valid profile resolution, missing profile, unknown profile
- [x] go test ./... passes

## Notes
无
