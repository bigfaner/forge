---
status: "completed"
started: "2026-05-14 01:24"
completed: "2026-05-14 01:35"
time_spent: "~11m"
---

# Task Record: 3.3 Probe command + justfile cleanup

## Summary
Created forge probe top-level command (probe.go) that delegates to e2eprobe.ProbeServers() with optional path arg (default /health). Modified ProbeServers to accept path parameter and match acceptance criteria output messages. Cleaned up justfile by removing 6 migrated recipes (test-e2e, probe, e2e-setup, e2e-verify, e2e-compile, e2e-discover) and 5 duplicate profile detection bash blocks. Added check-stale-refs recipe for CI stale reference detection. Bumped version to 3.2.0.

## Changes

### Files Created
- forge-cli/internal/cmd/probe.go
- forge-cli/internal/cmd/probe_test.go

### Files Modified
- forge-cli/pkg/e2eprobe/e2eprobe.go
- forge-cli/pkg/e2eprobe/e2eprobe_test.go
- forge-cli/internal/cmd/root.go
- forge-cli/internal/cmd/root_test.go
- forge-cli/internal/cmd/quality_gate.go
- forge-cli/scripts/version.txt
- justfile

### Key Decisions
- Modified ProbeServers signature to accept path parameter (string) instead of creating a separate function, keeping the API clean
- runProbe exits with os.Exit(1) on failure, consistent with other Cobra Run functions in the codebase
- Used 'forge probe' as top-level command per tech design, not nested under any group

## Test Results
- **Tests Executed**: Yes
- **Passed**: 83
- **Failed**: 0
- **Coverage**: 83.7%

## Acceptance Criteria
- [x] probe.go created: Use='probe [path]', Args=cobra.MaximumNArgs(1), delegates to e2eprobe.ProbeServers()
- [x] Default path: /health
- [x] No config.yaml -> 'OK: CLI-only project'; server down -> exit 1 with 'FAIL: <url> not responding'
- [x] Registered as top-level command in root.go
- [x] Justfile: remove migrated e2e recipes (e2e-setup, e2e-verify, e2e-compile, e2e-discover, test-e2e) and probe recipe
- [x] Justfile: remove the 5 duplicate profile detection bash blocks (~30 lines of duplicated code)
- [x] Justfile: add just check-stale-refs recipe for CI stale reference detection
- [x] go test ./... passes

## Notes
无
