---
status: "completed"
started: "2026-05-14 00:44"
completed: "2026-05-14 00:50"
time_spent: "~6m"
---

# Task Record: 2.6 Fix empty template variable rendering in prompt templates

## Summary
Fixed empty template variable rendering in prompt.go. Added cleanTemplateOutput() post-processing step that removes residual artifacts when template variables are empty: (1) lines with empty backtick conditionals like 'If `` is non-empty', (2) label-only lines with empty values like 'SCOPE: ' or 'PROFILE: ', (3) trailing whitespace on 'just' command lines, (4) collapsed consecutive blank lines. Added 4 new tests covering empty/non-empty PHASE_SUMMARY, empty SCOPE, and empty PROFILE cases.

## Changes

### Files Created
无

### Files Modified
- forge-cli/pkg/prompt/prompt.go
- forge-cli/pkg/prompt/prompt_test.go
- forge-cli/scripts/version.txt

### Key Decisions
- Chose post-processing cleanup in cleanTemplateOutput() rather than modifying 8+ template files -- single point of fix handles all current and future empty-variable edge cases
- The cleanup runs after all variable substitutions, keeping the replacement logic simple and the cleanup logic isolated

## Test Results
- **Tests Executed**: Yes
- **Passed**: 16
- **Failed**: 0
- **Coverage**: 88.9%

## Acceptance Criteria
- [x] When a template variable is empty, the line containing only that variable is removed entirely
- [x] When a template variable is empty, any line containing a conditional sentence referencing that variable in backticks is removed entirely
- [x] When the variable has a value, behavior is unchanged (variable replaced, conditional sentence preserved with the value)
- [x] just compile, just fmt, just lint, just test all pass
- [x] Existing tests pass; new test(s) cover the empty-variable and non-empty-variable cases

## Notes
无
