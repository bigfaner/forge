---
id: "2.2"
title: "Rename commands + delete template"
priority: "P0"
estimated_time: "2h"
dependencies: ["1.gate", "2.1"]
scope: "backend"
breaking: true
type: "implementation"
mainSession: false
---

# 2.2: Rename commands + delete template

## Description

Rename 6 Cobra commands by updating their `Use` strings and file names. Delete the `template` command and its test. These are mechanical changes that update the CLI interface to match the new naming convention.

## Reference Files

- `design/tech-design.md` §Interfaces — 2. Renamed Commands (full table with old/new Use strings)
- `design/tech-design.md` §Interfaces — 8. Deleted Command: template
- `design/tech-design.md` §Interfaces — 2b. Unchanged Commands (reference for existing Cobra structs)

## Acceptance Criteria

- [ ] `record.go` → `submit.go`: `Use: "submit"`, behavior unchanged
- [ ] `check.go` → `check_deps.go`: `Use: "check-deps"`, behavior unchanged
- [ ] `validate.go` → `validate_index.go`: `Use: "validate-index"`, behavior unchanged
- [ ] `verify_completion.go` → `verify_task_done.go`: `Use: "verify-task-done"`, behavior unchanged
- [ ] `all_completed.go` → `quality_gate.go`: `Use: "quality-gate"`, behavior unchanged (cap added in task 2.4)
- [ ] `template.go` and `template_test.go` deleted — no callers found
- [ ] All existing tests updated with new command names/paths
- [ ] `go test ./...` passes with 0 failures

## User Stories

- Story 1: `forge task submit` is self-explanatory (vs `task record` which was ambiguous)
- Story 3: `forge task submit` clearly expresses the action of submitting task results
- Story 4: `forge quality-gate` clearly indicates quality gate function (vs `task all-completed`)

## Implementation Notes

Only Cobra `Use` strings and file names change — no behavioral changes. The `Short` descriptions can be updated to match the new naming if they reference the old command name. Existing error messages remain unchanged per design spec. The template command deletion requires verifying no callers exist — search for `template` in all skill/command/hook files.
