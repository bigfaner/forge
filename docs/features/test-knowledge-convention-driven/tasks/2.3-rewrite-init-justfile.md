---
id: "2.3"
title: "Rewrite init-justfile skill"
priority: "P0"
estimated_time: "2h"
dependencies: ["1.gate"]
scope: "all"
breaking: false
type: "coding.feature"
mainSession: false
---

# 2.3: Rewrite init-justfile skill

## Description
Rewrite the init-justfile SKILL.md to generate e2e recipes from Convention + LLM knowledge instead of `forge test get justfile`. Remove `forge test detect` and `forge test get justfile` calls.

## Reference Files
- `plugins/forge/skills/init-justfile/SKILL.md` — current skill (to rewrite)
- `docs/features/test-knowledge-convention-driven/design/tech-design.md` — I-6 init-justfile interface
- `docs/features/test-knowledge-convention-driven/prd/prd-spec.md` — FS-2, FS-3

## Acceptance Criteria
- SKILL.md contains zero references to `forge test detect`, `forge test get justfile`
- Step 0: List `docs/conventions/` → load Framework section
- Step 1: Generate e2e-compile/e2e-test/e2e-setup recipes from Convention + LLM knowledge
- No Convention → LLM generates recipes from file signal detection (cold start)
- Generated justfile includes e2e-compile, e2e-test, e2e-setup recipes
- If justfile already exists with e2e recipes → skip, use existing

## User Stories
- Story 3 (cold start): LLM generates recipes from defaults
- Story 8 (init-justfile normal): Convention-driven recipe generation

## Hard Rules
- No framework-specific recipe templates — LLM generates based on Convention description

## Implementation Notes
- Cold start: LLM uses conservative strategy for e2e-compile recipe (most common patterns per language)
- Warm start: Convention Framework section provides enough info for correct recipe generation
- If justfile exists but missing e2e recipes → append only the missing recipes
