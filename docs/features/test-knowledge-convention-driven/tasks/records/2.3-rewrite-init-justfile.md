---
status: "completed"
started: "2026-05-20 02:21"
completed: "2026-05-20 02:24"
time_spent: "~3m"
---

# Task Record: 2.3 Rewrite init-justfile skill

## Summary
Rewrite init-justfile SKILL.md to generate e2e recipes from Convention + LLM knowledge instead of forge test detect/get-justfile. Removed all forge test detect and forge test get justfile references. Added Convention loading step (Step 0) that reads Framework/Tags/Result-Format sections from docs/conventions/. Added LLM-driven e2e-compile/e2e-test/e2e-setup recipe generation from Convention content. Added cold start fallback when no Convention files exist (LLM generates from file signal detection). Added skip logic when justfile already contains e2e recipes. Added append-only logic for missing e2e recipes. Removed --lang parameter and framework-specific template selection.

## Changes

### Files Created
无

### Files Modified
- plugins/forge/skills/init-justfile/SKILL.md

### Key Decisions
- Removed --lang parameter entirely since recipe generation is now Convention-driven, not template-driven
- Added e2e-compile to Standard Target Contract (was missing from original)
- Cold start uses conservative LLM defaults per detected language rather than hardcoded templates, satisfying the Hard Rule against framework-specific recipe templates
- Removed project-type target from Standard Target Contract (no longer relevant without profile system)

## Test Results
- **Tests Executed**: No
- **Passed**: 0
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] SKILL.md contains zero references to forge test detect, forge test get justfile
- [x] Step 0: List docs/conventions/ and load Framework section
- [x] Step 1: Generate e2e-compile/e2e-test/e2e-setup recipes from Convention + LLM knowledge
- [x] No Convention -> LLM generates recipes from file signal detection (cold start)
- [x] Generated justfile includes e2e-compile, e2e-test, e2e-setup recipes
- [x] If justfile already exists with e2e recipes -> skip, use existing
- [x] No framework-specific recipe templates (Hard Rule)

## Notes
SKILL.md rewrite only (markdown file). No Go code changed. Static checks (compile, fmt, lint) all pass. Coverage set to -1.0 as this is a skill definition file, not testable Go code.
