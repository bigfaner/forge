---
status: "completed"
started: "2026-05-20 08:00"
completed: "2026-05-20 08:02"
time_spent: "~2m"
---

# Task Record: 2.1 Rewrite gen-test-scripts skill

## Summary
Rewrite gen-test-scripts SKILL.md to use Convention files + Code Reconnaissance instead of Profile system. Verified all acceptance criteria: zero forge test CLI references, Step 0 Convention loading with domains filtering, Step 1 Code Reconnaissance with framework pattern scanning, Step 2 Contract reading, Step 3 code generation driven by Convention content, Step 4 compile gate with retry (max 2) and recovery guidance. No framework branch logic (if framework == go). All framework knowledge comes from Convention files.

## Changes

### Files Created
无

### Files Modified
- plugins/forge/skills/gen-test-scripts/SKILL.md

### Key Decisions
- SKILL.md already contained the Convention-driven rewrite from a prior session. Verified all acceptance criteria are met rather than re-rewriting an already correct file.
- Pipeline structure: 5 steps (Step 0 Convention load -> Step 1 Reconnaissance -> Step 2 Read Contracts -> Step 3 Generate -> Step 4 Compile Gate) provides more granular guidance than the 4-step outline in Implementation Notes while covering all required functionality.
- Hard rule enforcement via HARD-RULE tags: no silent framework defaulting, no fabricated Fact Table values, batch generation per Journey, no staging area.

## Test Results
- **Tests Executed**: No
- **Passed**: 0
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] SKILL.md contains zero references to forge test detect, forge test get, forge test framework
- [x] Step 0: List docs/conventions/ -> load files matching domains: [testing, ...]
- [x] Step 1: Code Reconnaissance — scan existing test files for framework patterns
- [x] Step 2: Generate code using Convention + Reconnaissance + Skill methodology
- [x] Step 3: Compile gate just e2e-compile -> retry on failure (max 2)
- [x] Convention validation: missing sections -> warning + LLM defaults; malformed file -> skip with warning
- [x] No Convention found -> output hint, proceed with LLM defaults
- [x] Compile gate failure recovery: output error + file path + recovery suggestions
- [x] No if framework == branch logic — framework differences entirely described by Convention content
- [x] Skill methodology stays in SKILL.md; framework knowledge moves to Convention

## Notes
Prompt-only rewrite task (markdown SKILL.md). No Go code changes. Static checks (compile, fmt, lint) all pass. The SKILL.md was already rewritten in a prior session; this task verified all acceptance criteria are met.
