---
status: "completed"
started: "2026-05-20 02:14"
completed: "2026-05-20 02:17"
time_spent: "~3m"
---

# Task Record: 2.1 Rewrite gen-test-scripts skill

## Summary
Rewrite gen-test-scripts SKILL.md to use Convention-driven architecture instead of Profile system. Removed all forge test CLI command references, added Convention loading with validation (Step 0), Code Reconnaissance with framework pattern scanning (Step 1), Contract reading (Step 2), Code generation (Step 3), and compile gate with retry mechanism via just e2e-compile (Step 4). Removed hardcoded framework branch logic (@feature tag table) per Hard Rules — framework differences are now entirely described by Convention content.

## Changes

### Files Created
无

### Files Modified
- plugins/forge/skills/gen-test-scripts/SKILL.md

### Key Decisions
- Restructured pipeline from 5 steps (0-4) to 4 steps (0-4) with clearer separation: Step 0 (Convention Load + Validation), Step 1 (Code Reconnaissance with framework columns), Step 2 (Read Contracts), Step 3 (Generate Code), Step 4 (Compile Gate with retry)
- Removed hardcoded @feature tag format table — this was framework branch logic prohibited by Hard Rules. Tags now come from Convention's Tags section exclusively
- Added Convention validation in Step 0.3 with explicit handling for missing sections, invalid content, overlapping domains, and malformed files
- Extended Code Reconnaissance Fact Table with framework-specific columns: file-pattern, assertion-lib, build-tag, naming-pattern
- Replaced framework-specific compile commands (go build, pytest --collect-only, tsc) with unified just e2e-compile gate
- Added explicit retry mechanism: 3 attempts total (initial + 2 retries), with error feedback loop and recovery guidance on exhaustion

## Test Results
- **Tests Executed**: Yes
- **Passed**: 0
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] SKILL.md contains zero references to forge test detect, forge test get, forge test framework
- [x] Step 0: List docs/conventions/ → load files matching domains: [testing, ...]
- [x] Step 1: Code Reconnaissance — scan existing test files for framework patterns (imports, tags, naming)
- [x] Step 2: Generate code using Convention + Reconnaissance + Skill methodology
- [x] Step 3: Compile gate just e2e-compile → retry on failure (max 2)
- [x] Convention validation: missing sections → warning + LLM defaults; malformed file → skip with warning
- [x] No Convention found → output hint, proceed with LLM defaults
- [x] Compile gate failure recovery: output error + file path + recovery suggestions

## Notes
Pure skill prompt rewrite — no Go code changes. Hard Rules verified: no framework branch logic in SKILL.md, framework knowledge fully delegated to Convention files.
