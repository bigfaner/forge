---
id: "4.1"
title: "Regression validation (126+ tests)"
priority: "P0"
estimated_time: "2h"
dependencies: ["3.gate"]
scope: "all"
breaking: false
type: "gate"
mainSession: false
---

# 4.1: Regression validation (126+ tests)

## Description
Run the full regression suite against forge-cli's 126+ existing e2e tests. All tests must pass without modification. This validates that the Convention model produces equivalent results to the Profile model for the default Go testing setup.

## Reference Files
- `tests/e2e/` — existing e2e test directory
- `docs/features/test-knowledge-convention-driven/prd/prd-spec.md` — backward compatibility goal

## Acceptance Criteria
- `just e2e-compile` passes for all 126+ existing test files
- `just e2e-test` passes for all 126+ existing test files
- No test file modifications required
- First-pass compile rate >= 85% on gen-test-scripts regeneration of all Journeys

## User Stories
- Story 4 (backward compatibility): all existing tests pass without modification

## Hard Rules
- If any test fails, create a P0 fix task before proceeding to 4.2

## Implementation Notes
- Run `just e2e-compile` first to verify compilation
- Run `just e2e-test` to verify execution
- For compile rate measurement: regenerate all Journeys using Convention-driven gen-test-scripts and count first-pass successes
