---
id: "1.5"
title: "Delete pkg/profile + import audit"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.4"]
scope: "backend"
breaking: false
type: "coding.cleanup"
mainSession: false
---

# 1.5: Delete pkg/profile + import audit

## Description
Delete the entire `pkg/profile/` directory (9 Go files + 6 language subdirectories with embedded content). Run import audit to verify zero references remain. Update or remove `docs/conventions/profile-system.md` convention file.

## Reference Files
- `forge-cli/pkg/profile/` — entire directory to delete
- `docs/conventions/profile-system.md` — convention file about Profile system (to update or remove)

## Acceptance Criteria
- `pkg/profile/` directory does not exist
- `grep -r "pkg/profile" forge-cli/` returns zero results
- `go build ./...` succeeds
- `go test ./...` passes (all existing tests)
- `docs/conventions/profile-system.md` updated to reflect Convention-driven model (or removed if superseded)
- No orphaned references to "profile" in CLI help text or error messages

## User Stories
- Story 4 (backward compatibility): upgrade experience is clean

## Hard Rules
- Import audit is mandatory — must pass before this task is marked complete

## Implementation Notes
- Delete entire `pkg/profile/` directory including: config.go, detect.go, embed.go, framework.go, tests, and `languages/` subdirectory with all 6 embedded languages
- Run `grep -r "pkg/profile" forge-cli/` as verification step
- Check for any remaining references to profile-related strings in CLI help text
- Update go.sum/go.mod if embed changes affect module checksums
