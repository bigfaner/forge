---
id: "3.1.2"
title: "Add cobra Args validation to commands"
priority: "P2"
estimated_time: "30min"
dependencies: ["3.1.1"]
type: "coding.refactor"
scope: "backend"
---

# 3.1.2: Add Args validation

## Description

Add `Args: cobra.NoArgs` or `Args: cobra.MaximumNArgs(n)` to all cobra commands that currently lack args validation.

## Acceptance Criteria

- [ ] All commands have appropriate `.Args` validators
- [ ] `cobra.NoArgs` for commands taking no positional arguments
- [ ] `cobra.MaximumNArgs(1)` or similar for commands that accept optional args
- [ ] `go build ./...` passes

## Hard Rules

- Do NOT change command behavior — only add validation