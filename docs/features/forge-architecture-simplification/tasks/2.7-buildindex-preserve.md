---
id: "2.7"
title: "BuildIndex orphan cleanup + PreserveRuntimeFields"
priority: "P1"
estimated_time: "2h"
dependencies: ["1.gate", "2.3"]
scope: "backend"
breaking: false
type: "coding.enhancement"
mainSession: false
---

# 2.7: BuildIndex orphan cleanup + PreserveRuntimeFields

## Description

Modify BuildIndex to default-cleanup orphans (currently only warns). Add fix-task exemption from orphan detection. Create `pkg/task/preserve.go` with explicit field assignment for runtime-only fields during re-index.

## Reference Files

- design/tech-design.md — Interface 3: Preserve Runtime Fields (implementation shown)
- design/tech-design.md — PRD Coverage Map § BC-8, BC-9, BC-10

## Acceptance Criteria

- [ ] `PreserveRuntimeFields(existing, newTask *Task)` implemented with explicit field assignment (Status, SourceTaskID, BlockedReason)
- [ ] BuildIndex calls PreserveRuntimeFields during re-index
- [ ] Orphans cleaned up by default (printed warning remains)
- [ ] Fix-tasks (auto-generated) exempted from orphan detection
- [ ] Characterization test `TestBuildIndex_Orphan_WarningOnly` updated to expect cleanup
- [ ] `go build ./...` + tests pass

## User Stories

No direct user story mapping.

## Hard Rules

- PreserveRuntimeFields uses explicit field assignment, NOT reflection, NOT string slices.
- Adding a new preserved field = adding one case line (compile-time safety).

## Implementation Notes

See design Interface 3 for the exact implementation of PreserveRuntimeFields. For orphan cleanup, change the warning-only path to also remove the orphan entry from the index. For fix-task exemption, use `isAutoGenTaskID` check to skip auto-generated tasks.
