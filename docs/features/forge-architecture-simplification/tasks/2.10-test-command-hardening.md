---
id: "2.10"
title: "Test command hardening"
priority: "P1"
estimated_time: "1.5h"
dependencies: ["1.gate", "2.2"]
scope: "backend"
breaking: true
type: "coding.enhancement"
mainSession: false
---

# 2.10: Test command hardening

## Description

Harden test commands: `test promote` rejects path traversal (`../`), `test verify` returns errors on parse failure (not zero-value) and marks unverifiable contracts (not OK).

## Reference Files

- design/tech-design.md — EC-6, EC-7, EC-8
- `forge-cli/internal/cmd/test_promote.go` — no path traversal check
- `forge-cli/internal/cmd/test_verify.go` — silent zero-value, OK on no Fact Table

## Acceptance Criteria

- [ ] `test_promote.go`: `../` in journey name → `ErrInvalidPath` AIError (exit 2)
- [ ] `test_verify.go`: parse failure → `ErrEvalParseFailure` error (exit 1), not silent zero-value
- [ ] `test_verify.go`: no Fact Table → `ErrContractUnverifiable` (exit 2), not "OK"
- [ ] `os.Exit(1)` calls replaced with AIError + Exit()
- [ ] `go build ./...` + tests pass

## Hard Rules

- Path traversal check uses `filepath.Base()` + reject `..` components. Do NOT use regex.

## Implementation Notes

`test_verify.go` is strictly read-only — no file locking needed. `test_promote.go` modifies test files (not index.json), so crash-safety risk is limited.
