---
id: "1.2"
title: "Naming fixes (testgen → autogen + type renames)"
priority: "P1"
estimated_time: "2h"
dependencies: ["0.1"]
scope: "backend"
breaking: false
type: "coding.refactor"
mainSession: false
---

# 1.2: Naming fixes (testgen → autogen + type renames)

## Description

Rename types, functions, and files to reflect their current responsibilities. Zero behavior change.

Key renames:
- `pkg/task/testgen.go` → `pkg/task/autogen.go`
- `TestTaskDef` → `AutoGenTaskDef`
- Related function/type renames to match

## Reference Files

- design/tech-design.md — CH-1: Naming accuracy
- `forge-cli/pkg/task/testgen.go` — primary rename target

## Acceptance Criteria

- [ ] `testgen.go` renamed to `autogen.go` (file rename via `git mv`)
- [ ] `TestTaskDef` renamed to `AutoGenTaskDef` (and all references)
- [ ] All import paths updated across `internal/` and `pkg/`
- [ ] `go build ./...` passes
- [ ] Characterization tests (0.1) still pass unchanged
- [ ] No behavior change — pure rename

## Hard Rules

- MUST NOT change any function logic — only rename
- Use `git mv` for file renames to preserve history
- Run `go build ./...` after each rename to catch missed references

## Implementation Notes

Use IDE refactoring or `gofmt -r` for bulk renames. Verify with `grep -r` for old names after completion.
