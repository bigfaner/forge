---
id: "1.2"
title: "Naming fixes (13 items)"
priority: "P1"
estimated_time: "2h"
dependencies: ["0.1"]
scope: "backend"
breaking: false
type: "coding.refactor"
mainSession: false
---

# 1.2: Naming fixes (13 items)

## Description

Rename types, files, and variables to reflect current responsibilities. Zero behavior change — characterization tests must pass unchanged. Key renames: TestTaskDef→AutoGenTaskDef, testgen.go→autogen.go.

## Reference Files

- design/tech-design.md — PRD Coverage Map § CH-1
- prd/prd-spec.md — Goal § CH-1, Value Domain 7 § CH-1

## Acceptance Criteria

- [ ] All 13 naming items from PRD CH-1 completed
- [ ] `go build ./...` compiles without error
- [ ] `go vet ./...` passes
- [ ] All Phase 0 characterization tests pass unchanged
- [ ] Git tag `phase-1-naming-baseline` created after completion

## User Stories

No direct user story mapping.

## Hard Rules

- Use `gofmt` after renaming to ensure formatting consistency.
- Use Go's `gorename` or IDE refactoring to ensure all references updated.
- Create git tag `phase-1-naming-baseline` after all Phase 1 tasks complete (coordinate with 1.1 and 1.3).

## Implementation Notes

Primary renames: TestTaskDef→AutoGenTaskDef, testgen.go→autogen.go, and 11 other items from the design. Update all callers, test files, and import paths. Run `go build` after each rename to catch missed references immediately.
