---
id: "3.1"
title: "RunE migration + Args validation"
priority: "P2"
estimated_time: "3h"
dependencies: ["2.gate"]
scope: "backend"
breaking: true
type: "coding.refactor"
mainSession: false
---

# 3.1: RunE migration + Args validation

## Description

Migrate remaining `Run` commands to `RunE` for consistent error propagation. Add `.Args` validation (cobra.NoArgs / MaximumNArgs) to commands currently missing it. Unify output with PrintBlock.

## Reference Files

- design/tech-design.md — CC-1, CC-3, CC-4

## Acceptance Criteria

- [ ] All remaining `Run:` commands converted to `RunE:` returning `error`
- [ ] Commands with missing `.Args` have appropriate args validators
- [ ] All command output uses PrintBlock formatting
- [ ] No `os.Exit()` calls in command functions (all use Exit() or return error)
- [ ] `go build ./...` + `go test ./...` pass

## Hard Rules

- Do NOT change command behavior during migration — only error propagation mechanism

## Implementation Notes

Mechanical refactoring. For each command: change `Run: func(cmd *cobra.Command, args []string) { ... }` to `RunE: func(cmd *cobra.Command, args []string) error { ... }`. Replace `os.Exit()` with `return error`. Add `Args: cobra.NoArgs` or `Args: cobra.MaximumNArgs(1)` as appropriate.
