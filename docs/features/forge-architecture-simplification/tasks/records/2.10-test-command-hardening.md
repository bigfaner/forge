---
status: "completed"
started: "2026-05-22 11:17"
completed: "2026-05-22 11:29"
time_spent: "~12m"
---

# Task Record: 2.10 Test command hardening

## Summary
Hardened test commands: added path traversal check in test promote, replaced os.Exit(1) with AIError+Exit() in both test_promote.go and test_verify.go, added ErrEvalParseFailure on parse failures and ErrContractUnverifiable on no contracts in test verify

## Changes

### Files Created
无

### Files Modified
- forge-cli/internal/cmd/test_promote.go
- forge-cli/internal/cmd/test_promote_test.go
- forge-cli/internal/cmd/test_verify.go
- forge-cli/internal/cmd/test_verify_test.go

### Key Decisions
- Path traversal check uses filepath.Base() + strings.Contains for '..' per Hard Rules, not regex
- Extracted validateJourneyName helper for testability — runTestPromote calls Exit() which uses os.Exit() and can't be easily unit tested
- No-contracts case handled by checking summary.Total == 0 after Verify() returns successfully, not by modifying contract.Verify() to return an error

## Test Results
- **Tests Executed**: Yes
- **Passed**: 55
- **Failed**: 0
- **Coverage**: 6.5%

## Acceptance Criteria
- [x] test_promote.go: ../ in journey name → ErrInvalidPath AIError (exit 2)
- [x] test_verify.go: parse failure → ErrEvalParseFailure error (exit 1), not silent zero-value
- [x] test_verify.go: no Fact Table → ErrContractUnverifiable (exit 2), not OK
- [x] os.Exit(1) calls replaced with AIError + Exit()
- [x] go build ./... + tests pass

## Notes
Pre-existing test failures in characterization_test.go and add_cmd_test.go (task indexing and block-source logic) are unrelated to these changes. All test_promote and test_verify tests pass. Contract package tests also all pass.
