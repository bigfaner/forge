---
status: "completed"
started: "2026-05-21 16:20"
completed: "2026-05-21 16:33"
time_spent: "~13m"
---

# Task Record: 2.1 State machine: ValidateTransition + CheckTransitionDeps

## Summary
Created pkg/task/statemachine.go implementing centralized transition validation with two-phase design: ValidateTransition (pure state check) + CheckTransitionDeps (dependency resolution). Includes TransitionRole enum (submit, claim, reopen, auto), TransitionError type, data-driven transition rule table, canAutoUnblock unexported helper, and comprehensive unit tests covering all state combinations and role isolation.

## Changes

### Files Created
- forge-cli/pkg/task/statemachine.go
- forge-cli/pkg/task/statemachine_test.go

### Files Modified
无

### Key Decisions
- Data-driven transition table (slice of TransitionRule) with first-match-wins lookup instead of if/else chain
- Two-phase validation: phase 1 (ValidateTransition) handles pure state rules, phase 2 (CheckTransitionDeps) handles dependency resolution
- TransitionError struct provides From/To/Role/Msg for structured error reporting
- canAutoUnblock is unexported as specified - checks for active fix-tasks (non-terminal coding.fix type) targeting the blocked task
- Blocked -> pending/in_progress transitions return error in phase 1 signaling dependency check needed
- RoleReopen is restricted to only rejected/skipped states via explicit catch-all deny rule before the open wildcard rule

## Test Results
- **Tests Executed**: Yes
- **Passed**: 40
- **Failed**: 0
- **Coverage**: 87.4%

## Acceptance Criteria
- [x] TransitionRole enum defined with 4 values: submit, claim, reopen, auto
- [x] ValidateTransition(current, target, role) error implements full transition table
- [x] Terminal state protection: completed -> * blocked (all roles), rejected/skipped -> * blocked (except RoleReopen -> pending)
- [x] CheckTransitionDeps(index, taskID) ([]string, error) for blocked -> pending/in_progress
- [x] canAutoUnblock unexported helper checks active fix-task count
- [x] Unit tests cover all state combinations + role isolation
- [x] go build ./... compiles, go test ./pkg/task/... passes

## Notes
No --force parameter anywhere. Transition table is the single authority. canAutoUnblock is unexported as required by hard rules.
