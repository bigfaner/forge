---
status: "completed"
started: "2026-05-22 09:53"
completed: "2026-05-22 10:22"
time_spent: "~29m"
---

# Task Record: 2.6 Migrate index writers to WithLock pattern

## Summary
Migrated all index writers from direct task.SaveIndex (non-atomic, no lock) to index.WithLock + index.SaveIndexAtomic pattern. Modified 6 files: claim.go wraps claim-next mutation in WithLock; AddTask/AddDependency in add.go wrap full read-modify-write cycle in WithLock; build.go uses SaveIndexAtomic; index.go redundant save uses WithLock + SaveIndexAtomic; reopen.go already had caller-level WithLock, replaced internal SaveIndex with SaveIndexAtomic; migrate.go wraps full cycle in WithLock. Updated 2 test error scenarios (claim and reopen) to test WithLock failure (non-writable directory) instead of read-only index file (which no longer fails with atomic writes). Verified zero remaining task.SaveIndex calls in production code.

## Changes

### Files Created
无

### Files Modified
- forge-cli/pkg/task/add.go
- forge-cli/pkg/task/build.go
- forge-cli/internal/cmd/claim.go
- forge-cli/internal/cmd/index.go
- forge-cli/internal/cmd/reopen.go
- forge-cli/internal/cmd/migrate.go
- forge-cli/internal/cmd/integration_test.go
- forge-cli/internal/cmd/reopen_test.go

### Key Decisions
- AddTask and AddDependency in pkg/task/ use WithLock internally, covering all callers including quality_gate.go's addFixTask
- BuildIndex uses SaveIndexAtomic only (not WithLock) since it's an idempotent rebuild that reads .md files, not JSON
- claim.go read-only check (checkExistingTaskState) stays outside lock to minimize lock contention
- Saved test error assertions updated to reflect atomic write behavior: read-only index file no longer causes write failure (temp+rename bypasses file permissions on POSIX)
- index.go redundant save after BuildIndex is kept and migrated to WithLock+SaveIndexAtomic (surgical change scope)

## Test Results
- **Tests Executed**: Yes
- **Passed**: 1867
- **Failed**: 0
- **Coverage**: 83.7%

## Acceptance Criteria
- [x] internal/cmd/claim.go uses WithLock(indexPath, fn) for index writes
- [x] internal/cmd/add.go uses WithLock(indexPath, fn) for index writes
- [x] pkg/task/build.go uses SaveIndexAtomic for index save
- [x] internal/cmd/quality_gate.go addFixTask uses WithLock for index writes
- [x] No remaining calls to task.SaveIndex on index.json paths (grep verified)
- [x] go build ./... + go test ./... pass

## Notes
pkg/task coverage: 87.5%, pkg/index coverage: 82.5%, internal/cmd coverage: 81.0% (all maintain existing coverage levels). Only SaveIndex references remaining are the function definition in pkg/task/index.go and test fixture setup code.
