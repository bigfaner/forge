---
status: "completed"
started: "2026-05-21 16:35"
completed: "2026-05-21 17:01"
time_spent: "~26m"
---

# Task Record: 2.2 Error handling: new error codes + exit code differentiation

## Summary
Added 4 new error codes (ErrInvalidTransition, ErrInvalidPath, ErrEvalParseFailure, ErrContractUnverifiable) with factory functions, ExitCode() method for differentiated exit codes (1=retryable, 2=blocking), and updated Exit() to use ExitCode() instead of always os.Exit(1). All existing error behavior preserved.

## Changes

### Files Created
无

### Files Modified
- forge-cli/internal/cmd/errors.go
- forge-cli/internal/cmd/errors_test.go

### Key Decisions
- ExitCode() uses a switch on blocking codes (ErrInvalidTransition, ErrInvalidPath, ErrContractUnverifiable) returning 2, defaulting to 1 for all others including ErrEvalParseFailure
- Factory functions named NewErr* to distinguish from existing Err* pattern that takes no/minimal args
- verify_task_done.go left untouched as per hard rules - it already uses os.Exit(2) directly

## Test Results
- **Tests Executed**: Yes
- **Passed**: 689
- **Failed**: 0
- **Coverage**: 80.8%

## Acceptance Criteria
- [x] 4 new error codes: ErrInvalidTransition, ErrInvalidPath, ErrEvalParseFailure, ErrContractUnverifiable
- [x] 4 new factory functions with appropriate Message/Cause/Hint/Action fields
- [x] AIError.ExitCode() returns 2 for blocking errors, 1 for retryable
- [x] Exit(err) calls os.Exit(aiErr.ExitCode()) instead of always os.Exit(1)
- [x] Unit tests for exit code mapping
- [x] Existing error behavior unchanged
- [x] go build ./... compiles

## Notes
无
