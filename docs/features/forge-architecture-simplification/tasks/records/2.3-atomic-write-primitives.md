---
status: "completed"
started: "2026-05-21 17:02"
completed: "2026-05-21 17:21"
time_spent: "~19m"
---

# Task Record: 2.3 WithLock callback + atomic state writes

## Summary
Added WithLock callback wrapper in pkg/index/lock.go (wraps LockFile/UnlockFile with defer-based release and 5s timeout). Extracted AtomicWrite(path, data, perm) shared helper from SaveIndexAtomic. Converted all state file writes in pkg/feature/forge_state.go (WriteForgeState, EnsureForgeState, MarkFeatureCompleted) and pkg/task/state.go (SaveState) to use atomic temp+rename pattern. Changed ClearForgeState to write allCompleted=false instead of deleting the file.

## Changes

### Files Created
无

### Files Modified
- forge-cli/pkg/index/atomic.go
- forge-cli/pkg/index/lock.go
- forge-cli/pkg/index/lock_test.go
- forge-cli/pkg/feature/forge_state.go
- forge-cli/pkg/feature/forge_state_test.go
- forge-cli/pkg/task/state.go
- forge-cli/pkg/task/state_test.go

### Key Decisions
- Extracted AtomicWrite as a public helper in pkg/index/ so both SaveIndexAtomic and external packages (feature, task) can reuse the temp+rename pattern without code duplication
- ClearForgeState now writes allCompleted=false instead of deleting the file, preserving the workspace marker (.forge/state.json) for subagent project root detection
- pkg/index/ remains a leaf package -- it only depends on stdlib, no internal deps introduced
- AtomicWrite uses a naming pattern '.<basename>.tmp.*' for temp files to keep them co-located with the target for same-filesystem rename

## Test Results
- **Tests Executed**: Yes
- **Passed**: 329
- **Failed**: 0
- **Coverage**: 87.2%

## Acceptance Criteria
- [x] WithLock(indexPath string, fn func() error) error wraps LockFile/UnlockFile with 5s timeout
- [x] pkg/feature/forge_state.go: WriteForgeState/EnsureForgeState/MarkFeatureCompleted use atomic write (temp+rename)
- [x] pkg/task/state.go: SaveState uses atomic write (temp+rename)
- [x] pkg/feature/forge_state.go: ClearForgeState writes false instead of deleting file
- [x] Unit tests: WithLock concurrent access, lock conflict, atomic write crash safety
- [x] go build ./... compiles

## Notes
Coverage breakdown: pkg/index 82.5%, pkg/task 87.4%, pkg/feature 91.6%. The test for 'write permission denied' in state_test.go was updated because atomic write (temp+rename) bypasses per-file write permissions on Unix -- rename() only requires directory write permission. This is the correct crash-safe behavior.
