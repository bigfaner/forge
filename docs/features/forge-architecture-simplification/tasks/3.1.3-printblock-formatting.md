---
id: "3.1.3"
title: "Unify output with PrintBlock formatting"
priority: "P2"
estimated_time: "30min"
dependencies: ["3.1.2"]
type: "coding.refactor"
scope: "backend"
---

# 3.1.3: Unify PrintBlock formatting

## Description

Ensure all command output uses PrintBlock/PrintField/PrintWarning helpers instead of raw `fmt.Println` or `fmt.Printf`. No `os.Exit()` calls in command functions.

## Acceptance Criteria

- [ ] All command output uses PrintBlock formatting helpers
- [ ] No `os.Exit()` calls in command functions (use Exit() or return error)
- [ ] `go build ./...` + `go test ./...` pass

## Hard Rules

- Do NOT change command behavior — only output mechanism