---
id: "1.1"
title: "Delete dead code (9 unused exports)"
priority: "P1"
estimated_time: "1.5h"
dependencies: ["0.1"]
scope: "backend"
breaking: false
type: "cleanup"
mainSession: false
---

# 1.1: Delete dead code (9 unused exports)

## Description

Delete 9 exported functions that are never called (confirmed by grep). Zero behavior change — characterization tests must pass unchanged after deletion.

## Reference Files

- design/tech-design.md — PRD Coverage Map § CH-2
- prd/prd-spec.md — Goal § CH-2, Value Domain 7 § CH-2

## Acceptance Criteria

- [ ] All 9 unused exported functions removed
- [ ] `go build ./...` compiles without error
- [ ] `go vet ./...` passes
- [ ] All Phase 0 characterization tests pass unchanged
- [ ] Grep confirms zero references to each deleted function

## User Stories

No direct user story mapping.

## Hard Rules

- Run `grep -r` for each function name before deleting to confirm zero references.
- If a function has references, do NOT delete — report as blocked.

## Implementation Notes

The design identifies 9 dead exports but doesn't enumerate them. Identify them by scanning `forge-cli/pkg/` and `forge-cli/cmd/` for exported functions with zero call sites. Use `go vet` and manual grep.
