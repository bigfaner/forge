---
id: "1.3"
title: "Extract magic values to constants + export isBusinessTask + NI fixes"
priority: "P1"
estimated_time: "2h"
dependencies: ["0.1"]
scope: "backend"
breaking: false
type: "coding.refactor"
mainSession: false
---

# 1.3: Extract magic values to constants + export isBusinessTask + NI fixes

## Description

Three categories of cleanup:
1. **Magic values** (MV-1~MV-10): Extract 10 hardcoded runtime-affecting values to `pkg/constants/` or config
2. **Code dedup** (CO-2): Unify `isBusinessTask` to single canonical location in `pkg/task/`
3. **Newly introduced issues** (NI-1~NI-5): Fix 5 small issues from merged PRs

## Reference Files

- docs/proposals/forge-architecture-simplification/proposal.md — Pattern 15 (MV-1~MV-10), Pattern 9 (CO-2), Pattern 18 (NI-1~NI-5)
- forge-cli/internal/cmd/submit.go — MV-6 (Chinese `"无"` → `"None"`)
- forge-cli/internal/cmd/quality_gate.go — MV-1 (`maxFixTasksPerStep=3`)
- forge-cli/internal/cmd/validate_index.go — CO-2 (`isBusinessTask`)
- forge-cli/pkg/task/add.go — CO-2 (`isBusinessTaskID`)
- forge-cli/pkg/prompt/prompt.go — CO-2 (third `isBusinessTask` copy)
- forge-cli/internal/cmd/feature_complete.go — NI-1 (state case inconsistency)
- forge-cli/internal/cmd/worktree.go — NI-2, NI-5

## Acceptance Criteria

- [ ] `ForgeDir`/`ForgeConfigFileName` defined once in `pkg/constants/` (or `pkg/task/constants.go` if import cycle)
- [ ] `submit.go` has zero Chinese characters — `"无"` replaced with `"None"` (MV-6)
- [ ] `maxFixTasksPerStep`, `probeTimeout`, `lockTimeout` extracted to constants (MV-1~MV-5)
- [ ] Test model magic values extracted: `"tests"` path, `"exit code 0/1"`, `"CLAUDE_PROJECT_DIR="`, `"forge-journey-"` (MV-8~MV-10)
- [ ] `isBusinessTask` exists only in `pkg/task/` as exported `IsBusinessTask` (CO-2)
- [ ] `cmd/validate_index.go`, `pkg/prompt/prompt.go`, `pkg/task/add.go` all call the canonical `IsBusinessTask`
- [ ] `feature_complete.go` state strings case-consistent (NI-1)
- [ ] `worktree.go remove` flag hint corrected (NI-2)
- [ ] `worktree.go` pre-flight checks extracted to shared function (NI-5)
- [ ] `clean-code` SKILL.md scope detection uses `forge task` command (NI-3)
- [ ] `go build ./...` compiles, `go test ./...` passes (1591 tests)
- [ ] No behavior change — only extraction/consolidation

## User Stories

No direct user story mapping — internal code health improvement.

## Hard Rules

- If `pkg/constants/` creates an import cycle with `pkg/feature/` or `pkg/forgeconfig/`, move constants to `pkg/task/constants.go` instead. Verify with `go build` before committing.
- `isBusinessTask` dedup: three copies exist — `cmd/validate_index.go`, `pkg/prompt/prompt.go`, `pkg/task/add.go`. All must call the single `IsBusinessTask` in `pkg/task/`

## Implementation Notes

Check if `pkg/constants/` directory already exists. If not, create it with `forge.go` containing all extracted constants. The `defaultLockTimeout` in `pkg/index/lock.go` is already a constant — verify it matches the proposed unified constant location.
