---
id: "1.3"
title: "Extract magic values to constants + export IsBusinessTask"
priority: "P1"
estimated_time: "1.5h"
dependencies: ["0.1"]
scope: "backend"
breaking: false
type: "coding.refactor"
mainSession: false
---

# 1.3: Extract magic values to constants + export IsBusinessTask

## Description

Extract hardcoded magic values into named constants and consolidate duplicated utility functions. Extend existing `pkg/feature/constants.go` rather than creating a new package.

Specific changes:
- Magic values in `internal/cmd/` → constants in `pkg/feature/constants.go` or local const blocks
- `isBusinessTask` (duplicated in `internal/cmd/validate_index.go`, `pkg/prompt/prompt.go`, `pkg/task/add.go`) → export as `IsBusinessTask` in `pkg/task/`

## Reference Files

- design/tech-design.md — CH-3: Magic value extraction, CH-4: Code dedup
- `forge-cli/pkg/feature/constants.go` — existing constants location
- `forge-cli/pkg/index/lock.go` — lock timeout already a constant (5s)

## Acceptance Criteria

- [ ] `isBusinessTask` exported as `IsBusinessTask` in `pkg/task/` (canonical location)
- [ ] All 3 duplicates replaced with import from `pkg/task/`
- [ ] Magic values (timeouts, limits, string literals) extracted to named constants in `pkg/feature/constants.go`
- [ ] `go build ./...` passes — no import cycles
- [ ] Characterization tests (0.1) still pass unchanged
- [ ] No behavior change — constants have same values as previous literals

## Hard Rules

- MUST NOT create new `pkg/constants/` package — use existing `pkg/feature/constants.go`
- MUST NOT change runtime behavior — constants must equal previous literal values
- Verify no import cycles with `go build`

## Implementation Notes

Focus on runtime-affecting magic values first (timeouts, limits). Display strings are lower priority. The `LockTimeoutSeconds` in `pkg/index/lock.go` is already a local constant — can be referenced from `pkg/feature/constants.go` or left in place.
