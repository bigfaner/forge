---
id: "2.9"
title: "Eval pipeline safety"
priority: "P1"
estimated_time: "2h"
dependencies: ["1.gate", "2.2"]
scope: "all"
breaking: false
type: "coding.enhancement"
mainSession: false
---

# 2.9: Eval pipeline safety

## Description

Add backup/rollback to eval SKILL.md, inject project context into doc-reviser, add parse failure recovery, add scope validation to prevent reviser from editing files outside DOC_DIR.

## Reference Files

- design/tech-design.md — PRD Coverage Map § ES-1~4, Related Changes table
- prd/prd-spec.md — Value Domain 4: Eval Safety

## Acceptance Criteria

- [ ] `eval/SKILL.md`: Step 1 creates `DOC_DIR.bak` backup, final step restores on max-iteration failure
- [ ] `eval/SKILL.md`: Scorer parse failure halts pipeline with error (not crash/silent)
- [ ] `doc-reviser.md`: Receives `CONTEXT_CONTENT` input (same conventions/business-rules as scorer)
- [ ] `doc-reviser.md`: Programmatic scope validation rejects edits outside DOC_DIR
- [ ] Related changes from PRD table items 1-4 addressed

## User Stories

- Story 4: Eval Pipeline Never Loses Work — all 3 ACs: backup restore on failure, parse failure halt, reviser sees context

## Hard Rules

- Must read `docs/conventions/forge-distribution.md` before modifying any plugin files (CLAUDE.md MANDATORY rule).

## Implementation Notes

This task modifies skill markdown files (SKILL.md, doc-reviser.md) and potentially task-executor.md. These are plugin-level files under `plugins/forge/`. Follow the forge-distribution conventions for path resolution.
