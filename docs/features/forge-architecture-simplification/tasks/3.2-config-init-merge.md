---
id: "3.2"
title: "Config init merge (huh TUI)"
priority: "P2"
estimated_time: "2h"
dependencies: ["2.gate"]
scope: "backend"
breaking: true
type: "refactor"
mainSession: false
---

# 3.2: Config init merge (huh TUI)

## Description

Delete the bufio-based config init path, keep only the huh TUI path. Ensure the unified init exposes all auto configuration fields (e2eTest, consolidateSpecs, cleanCode, gitPush).

## Reference Files

- design/tech-design.md — PRD Coverage Map § CC-2, CE-3

## Acceptance Criteria

- [ ] Bufio config init code path deleted
- [ ] Single huh TUI init path handles all configuration
- [ ] All 4 auto fields (e2eTest, consolidateSpecs, cleanCode, gitPush) exposed in init wizard
- [ ] `go build ./...` + `go test ./...` pass

## User Stories

- Story 5: Complete Configuration Control — AC: config init configures all 4 auto fields

## Hard Rules

- Must preserve all existing init behavior that users rely on. Only remove the bufio code path.

## Implementation Notes

Identify both init paths (bufio and huh) in the codebase. Keep the huh path, extend it to cover auto fields, and delete the bufio path. Update any tests that reference the bufio path.
