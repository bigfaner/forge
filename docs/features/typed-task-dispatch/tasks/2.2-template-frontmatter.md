---
id: "2.2"
title: "Update task template frontmatter (add type, remove noTest)"
priority: "P0"
estimated_time: "1-2h"
dependencies: ["1.gate"]
status: pending
breaking: false
type: "implementation"
---

# 2.2: Update task template frontmatter (add type, remove noTest)

## Description

更新 `plugins/forge/skills/breakdown-tasks/templates/` 下所有任务模板的 frontmatter：新增 `type` 字段（固定值或占位符），移除 `noTest` 字段。

## Files Created/Modified

| Action | File |
|--------|------|
| Modify | `plugins/forge/skills/breakdown-tasks/templates/task.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/gate-task.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/phase-summary-task.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/gen-test-cases.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/eval-test-cases.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/gen-test-scripts.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/run-e2e-tests.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/graduate-tests.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/verify-regression.md` |
| Modify | `plugins/forge/skills/breakdown-tasks/templates/consolidate-specs.md` |

## Acceptance Criteria

- [ ] `task.md`：新增 `type: "{{TYPE}}"` 占位符，移除 `noTest: false`
- [ ] `gate-task.md`：新增 `type: "gate"` 固定值，移除 `noTest: false`
- [ ] `phase-summary-task.md`：新增 `type: "doc-generation.summary"` 固定值，移除 `noTest: true`
- [ ] `gen-test-cases.md`：新增 `type: "test-pipeline.gen-cases"` 固定值，移除 `noTest: true`
- [ ] `eval-test-cases.md`：新增 `type: "test-pipeline.eval-cases"` 固定值，移除 `noTest: true`
- [ ] `gen-test-scripts.md`：新增 `type: "test-pipeline.gen-scripts"` 固定值，移除 `noTest: false`
- [ ] `run-e2e-tests.md`：新增 `type: "test-pipeline.run"` 固定值，移除 `noTest: false`
- [ ] `graduate-tests.md`：新增 `type: "test-pipeline.graduate"` 固定值，移除 `noTest: false`
- [ ] `verify-regression.md`：新增 `type: "test-pipeline.verify-regression"` 固定值，移除 `noTest: false`
- [ ] `consolidate-specs.md`：新增 `type: "doc-generation.consolidate"` 固定值，移除 `noTest: true`

## User Stories

Story 5（breakdown-tasks 自动设置 type）：
- Then 生成的 index.json 中所有任务均包含 type 字段，`task validate` 无报错

## Implementation Notes

- `task.md` 使用占位符 `{{TYPE}}`，由 breakdown-tasks skill 在生成时替换为具体值
- 其他模板使用固定值（类型已知）
- 移除 `noTest` 字段，不保留（模板是新建任务的起点，不需要向后兼容）
