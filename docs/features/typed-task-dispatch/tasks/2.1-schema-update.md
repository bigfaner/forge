---
id: "2.1"
title: "Update index.schema.json with type enum and blockedReason"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.gate"]
status: pending
breaking: true
type: "implementation"
---

# 2.1: Update index.schema.json with type enum and blockedReason

## Description

在 `plugins/forge/skills/breakdown-tasks/templates/index.schema.json` 中新增 `type` 枚举字段和 `blockedReason` 字段。`noTest` 字段标记为 deprecated（保留向后兼容，不删除）。

## Files Created/Modified

| Action | File |
|--------|------|
| Modify | `plugins/forge/skills/breakdown-tasks/templates/index.schema.json` |

## Acceptance Criteria

- [ ] `type` 字段已添加到 task properties，包含 11 个合法枚举值
- [ ] `blockedReason` 字段已添加到 task properties（string 类型）
- [ ] `noTest` 字段保留但 description 标记为 "Deprecated: use type field instead"
- [ ] 现有合法 index.json 仍通过 schema 验证（向后兼容）
- [ ] 新增 type 字段的 index.json 通过 schema 验证

## User Stories

Story 4（迁移旧 index.json）：`task validate` 对迁移后的 index.json 无报错。
Story 5（breakdown-tasks 自动设置 type）：生成的 index.json 通过 schema 验证。

## Implementation Notes

- type 枚举值：implementation, doc-generation.summary, doc-generation.consolidate, test-pipeline.gen-cases, test-pipeline.eval-cases, test-pipeline.gen-scripts, test-pipeline.run, test-pipeline.graduate, test-pipeline.verify-regression, fix, gate
- `noTest` 不删除（现有 index.json 可能仍含此字段），只在 description 中标注 deprecated
- breaking: true 因为这是 schema 变更，影响所有使用 breakdown-tasks 的下游
