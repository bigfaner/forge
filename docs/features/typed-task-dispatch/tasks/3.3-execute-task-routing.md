---
id: "3.3"
title: "Update execute-task.md routing to match run-tasks"
priority: "P0"
estimated_time: "1h"
dependencies: ["3.2"]
status: pending
breaking: false
type: "implementation"
---

# 3.3: Update execute-task.md routing to match run-tasks

## Description

更新 `plugins/forge/commands/execute-task.md`，与 run-tasks 保持相同路由逻辑：调用 `task prompt <id>` 合成 prompt，eval-cases 在主会话执行，record 缺失时调用 `--fix-record-missed`，不再使用 TASK_FILE + NO_TEST 参数组合。

## Files Created/Modified

| Action | File |
|--------|------|
| Modify | `plugins/forge/commands/execute-task.md` |

## Acceptance Criteria

- [ ] execute-task 调用 `task prompt <id>` 合成 prompt 后 dispatch 给 forge:task-executor
- [ ] eval-cases 类型在主会话中直接执行（不 dispatch subagent）
- [ ] record 缺失时调用 `task prompt <id> --fix-record-missed`
- [ ] 不再使用 TASK_FILE + NO_TEST 参数组合
- [ ] `task prompt` exit 非零时：标记任务 blocked，stderr 写入 blockedReason，不静默失败
- [ ] 路由逻辑与 run-tasks.md 完全一致（相同 TYPE 判断，相同 error 处理）

## User Stories

Story 6（execute-task 路由与 run-tasks 一致）：
- Given index.json 中存在一个 type 为 "implementation" 的任务
- When 通过 execute-task 执行该任务
- Then execute-task 调用 `task prompt <id>` 合成 prompt 后 dispatch 给 forge:task-executor；task-executor 收到的 prompt 内容与 run-tasks 路径下对同一任务生成的 prompt 内容相同

## Implementation Notes

- 参照 3.2 任务完成后的 run-tasks.md 路由逻辑，逐步对齐 execute-task.md
- 3.3 依赖 3.2 完成，确保两者路由逻辑一致
