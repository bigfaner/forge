---
status: "completed"
started: "2026-05-11 19:52"
completed: "2026-05-11 19:57"
time_spent: "~5m"
---

# Task Record: 3.2 Update run-tasks.md routing to use task prompt

## Summary
Updated run-tasks.md Step 2 dispatch logic to call `task prompt <TASK_ID>` for synthesized prompt, added TYPE field extraction from claim output, added eval-cases permanent exception routing (main session execution), replaced error-fixer dispatch with `task prompt --fix-record-missed` recovery, and removed all forge:error-fixer references.

## Changes

### Files Created
无

### Files Modified
- plugins/forge/commands/run-tasks.md

### Key Decisions
- TYPE field extracted from claim output to enable eval-cases routing without MAIN_SESSION flag
- task prompt exit != 0 writes stderr to blockedReason and marks task blocked, then continues loop
- eval-cases type executes SYNTHESIZED_PROMPT directly in main session (not dispatched to subagent)
- All other types dispatch to forge:task-executor with SYNTHESIZED_PROMPT (no TASK_FILE or NO_TEST params)
- Record-missing recovery uses task prompt --fix-record-missed instead of forge:error-fixer

## Test Results
- **Tests Executed**: Yes
- **Passed**: 13
- **Failed**: 0
- **Coverage**: 93.6%

## Acceptance Criteria
- [x] Step 2 calls `task prompt <TASK_ID>` to get synthesized prompt
- [x] `task prompt` exit non-zero: write stderr to blockedReason, `task status <KEY> blocked`, continue loop
- [x] type == test-pipeline.eval-cases: execute in main session (no subagent dispatch)
- [x] Other types: Agent(forge:task-executor, prompt=<stdout>), no TASK_FILE or NO_TEST params
- [x] Record missing recovery: `task prompt <TASK_ID> --fix-record-missed` → Agent(forge:task-executor)
- [x] All forge:error-fixer dispatch calls removed
- [x] claim output parsing adds TYPE field extraction
- [x] MAIN_SESSION routing preserved (eval-cases via TYPE, not MAIN_SESSION field)

## Notes
Doc-only task modifying run-tasks.md command file. Go tests are pre-existing and all pass. Coverage 93.6% is aggregate across all 12 packages with statements.
