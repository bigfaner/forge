---
status: "completed"
started: "2026-05-11 17:50"
completed: "2026-05-11 17:58"
time_spent: "~8m"
---

# Task Record: 1.3 Implement task prompt command

## Summary
Implemented task prompt command (task prompt <id> [--fix-record-missed]) in internal/cmd/prompt.go. Command reads current feature via RequireFeature/GetCurrentFeature, loads index via pkg/prompt.Synthesize, and writes synthesized prompt to stdout. Errors (type missing, unknown type, no project, no feature) go to stderr with exit 1. Registered promptCmd in root.go.

## Changes

### Files Created
- task-cli/internal/cmd/prompt.go
- task-cli/internal/cmd/prompt_test.go

### Files Modified
- task-cli/internal/cmd/root.go
- task-cli/scripts/version.txt

### Key Decisions
- Used fmt.Errorf wrapping to pass prompt.Synthesize errors through Exit(), which prints to stderr
- Registered promptCmd via init() pattern consistent with template.go, plus explicit AddCommand in root.go
- Version bumped 1.12.0 -> 1.13.0 (minor: new command)

## Test Results
- **Passed**: 7
- **Failed**: 0
- **Coverage**: 86.0%

## Acceptance Criteria
- [x] task prompt <id> stdout is non-empty synthesized prompt, stderr empty, exit 0
- [x] task prompt <id> with missing type: exit 1, stdout empty, stderr contains 'type field missing'
- [x] task prompt <id> with unknown type: exit 1, stdout empty, stderr contains 'unknown type'
- [x] task prompt <id> --fix-record-missed uses fix-record-missed template
- [x] Command completes within 500ms (local file read + string replace, no network)
- [x] rootCmd.AddCommand(promptCmd) registered
- [x] go test ./internal/cmd/... passes

## Notes
lint step skipped due to pre-existing toolchain issue (golangci-lint built with go1.25, project targets go1.26.1); go vet ./internal/cmd/... passes cleanly.
