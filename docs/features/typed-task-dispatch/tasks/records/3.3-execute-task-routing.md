---
status: "completed"
started: "2026-05-11 19:58"
completed: "2026-05-11 20:03"
time_spent: "~5m"
---

# Task Record: 3.3 Update execute-task.md routing to match run-tasks

## Summary
Updated execute-task.md routing to match run-tasks.md: replaced TASK_FILE+NO_TEST dispatch with task prompt synthesis, added TYPE-based routing (eval-cases executes in main session, all others dispatch to forge:task-executor), added task prompt --fix-record-missed recovery, added task prompt exit != 0 error handling that marks task blocked, removed old MAIN_SESSION Step 0 inline logic in favor of delegating to task document instructions.

## Changes

### Files Created
无

### Files Modified
- plugins/forge/commands/execute-task.md

### Key Decisions
- TYPE field extracted from claim output to enable eval-cases routing without MAIN_SESSION flag
- task prompt exit != 0 writes stderr to blockedReason and marks task blocked, then stops
- eval-cases type executes SYNTHESIZED_PROMPT directly in main session (not dispatched to subagent)
- All other types dispatch to forge:task-executor with SYNTHESIZED_PROMPT (no TASK_FILE or NO_TEST params)
- Record-missing recovery uses task prompt --fix-record-missed instead of forge:error-fixer
- Breaking task gate renumbered to Step 4 to match the 4-step flow (no Step 5 commit in execute-task)

## Test Results
- **Tests Executed**: Yes
- **Passed**: 13
- **Failed**: 0
- **Coverage**: 93.6%

## Acceptance Criteria
- [x] execute-task 调用 task prompt <id> 合成 prompt 后 dispatch 给 forge:task-executor
- [x] eval-cases 类型在主会话中直接执行（不 dispatch subagent）
- [x] record 缺失时调用 task prompt <id> --fix-record-missed
- [x] 不再使用 TASK_FILE + NO_TEST 参数组合
- [x] task prompt exit 非零时：标记任务 blocked，stderr 写入 blockedReason，不静默失败
- [x] 路由逻辑与 run-tasks.md 完全一致（相同 TYPE 判断，相同 error 处理）

## Notes
Doc-only task modifying execute-task.md command file. Go tests are pre-existing and all pass. Lint step failed due to pre-existing toolchain version mismatch (golangci-lint go1.25 vs project go1.26.1), unrelated to this change. Coverage 93.6% is aggregate from prior test run.
