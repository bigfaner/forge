---
status: "completed"
started: "2026-05-11 17:59"
completed: "2026-05-11 18:06"
time_spent: "~7m"
---

# Task Record: 1.4 Implement task migrate command

## Summary
Implemented task migrate command in task-cli/internal/cmd/migrate.go. The command reads the current feature's index.json, checks for any in_progress tasks (aborts with exit 1 if found), then calls prompt.InferType() for every task to fill the type field. Migration is idempotent. Registered migrateCmd in root.go. Bumped version to 1.14.0.

## Changes

### Files Created
- task-cli/internal/cmd/migrate.go
- task-cli/internal/cmd/migrate_test.go

### Files Modified
- task-cli/internal/cmd/root.go
- task-cli/scripts/version.txt

### Key Decisions
- Used prompt.InferType() directly (already implemented in 1.2) — no new inference logic needed
- Pre-flight check iterates all tasks before any mutation so index.json is never partially written
- Migration overwrites existing type fields (idempotent by design per task spec)

## Test Results
- **Passed**: 8
- **Failed**: 0
- **Coverage**: 86.1%

## Acceptance Criteria
- [x] task migrate infers correct type for all known ID patterns, task validate passes
- [x] task migrate exits 1 with stderr hint when in_progress task exists, index.json unchanged
- [x] task migrate prints 'Migrated N tasks. Run task validate to verify.' on stdout
- [x] All task statuses preserved after migration
- [x] rootCmd.AddCommand(migrateCmd) registered
- [x] go test ./internal/cmd/... passes

## Notes
golangci-lint skipped due to pre-existing toolchain version mismatch (go1.25 vs go1.26.1) — not introduced by this task.
