---
id: "3.1"
title: "Slim task-executor.md to constraint layer only"
priority: "P0"
estimated_time: "1h"
dependencies: ["2.gate"]
status: pending
breaking: false
type: "implementation"
---

# 3.1: Slim task-executor.md to constraint layer only

## Description

将 `plugins/forge/agents/task-executor.md` 从 259 行精简至 ~40 行，移除所有策略块（TDD 步骤、文档生成步骤、quality gate 步骤、NO_TEST 分支、MAIN_SESSION Guard），只保留执行约束层（Hard Constraints）。移除 inputs 字段（prompt 参数携带所有上下文）。

## Files Created/Modified

| Action | File |
|--------|------|
| Modify | `plugins/forge/agents/task-executor.md` |

## Acceptance Criteria

- [ ] task-executor.md 行数 ≤ 50 行
- [ ] 保留 5 条 Hard Constraints（ONE TASK、record-task 强制、无后台任务、最多 3 次 subagent、STOP 规则）
- [ ] 移除所有策略块：TDD 步骤、Doc Generation 步骤、Quality Gate 步骤、NO_TEST 分支
- [ ] 移除 MAIN_SESSION Guard（路由已由 run-tasks 在 dispatch 前处理）
- [ ] 移除 inputs 字段（TASK_KEY、TASK_ID、TASK_FILE、SCOPE、NO_TEST、PHASE_SUMMARY）
- [ ] 保留 name、description、model、color、memory frontmatter 字段

## User Stories

Story 1（非编码任务获得正确流程）：task-executor 不再包含 TDD 步骤，策略由 prompt 参数注入。
Story 7（error-fixer 废弃后等价覆盖）：task-executor 不再 dispatch error-fixer。

## Implementation Notes

精简后的 task-executor.md 结构：
```markdown
---
name: task-executor
description: "Thin executor: follow the steps in your prompt. Hard constraints always active."
model: sonnet
color: green
memory: project
---

## Hard Constraints

<EXTREMELY-IMPORTANT>
1. ONE TASK PER INVOCATION — after completing, STOP immediately, no exceptions
2. record-task IS MANDATORY — task is NOT done without it
3. NO BACKGROUND TASKS — all commands run synchronously
4. Maximum 3 subagent calls per task
5. FORBIDDEN: run "task claim", read index.json, or start any subsequent task
</EXTREMELY-IMPORTANT>

Execute the task described in your prompt. The prompt contains all steps and context.
Call forge:record-task when done. Then STOP.
```
