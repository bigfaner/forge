---
id: "4"
title: "Update skill type tables and docs-only fast path docs"
priority: "P2"
estimated_time: "30min"
dependencies: ["1"]
type: "doc"
mainSession: false
---

# 4: Update skill type tables and docs-only fast path docs

## Description

Update the Docs-Only Fast Path and auto-generation documentation in `quick-tasks/SKILL.md` and `breakdown-tasks/SKILL.md` to reflect the `doc.eval` → `doc.review` rename and the new mixed-feature routing behavior.

Note: `doc.review` is a system-internal type (auto-generated by `forge task index`), not a user-assignable type. The user-facing type tables do not need to include it. The updates are about documenting the auto-generation behavior correctly.

## Reference Files
- `docs/proposals/review-doc-pipeline/proposal.md` — Source proposal

## Affected Files

### Modify
| File | Changes |
|------|---------|
| `plugins/forge/skills/quick-tasks/SKILL.md` | Update Docs-Only Fast Path section to reference review-doc |
| `plugins/forge/skills/breakdown-tasks/SKILL.md` | Update Docs-Only Fast Path section to reference review-doc |

## Acceptance Criteria

- [ ] `quick-tasks/SKILL.md` Docs-Only Fast Path section references the auto-generated `T-review-doc` task (not `T-eval-doc`)
- [ ] `breakdown-tasks/SKILL.md` Docs-Only Fast Path section references the auto-generated `T-review-doc` task (not `T-eval-doc`)
- [ ] Both files document that mixed features (doc + coding) generate both T-review-doc and test pipeline tasks
- [ ] No references to `doc.eval`, `T-eval-doc`, or `eval-doc` remain in either skill file

## Hard Rules

- Do NOT add `doc.review` to the user-facing Type Assignment tables — it is system-internal only
- Keep changes minimal — only update references to the old naming

## Implementation Notes

Search for any mentions of `eval-doc`, `doc.eval`, `T-eval-doc` in both SKILL.md files and replace with the new `review-doc` naming. The key section is Docs-Only Fast Path which describes what happens when all tasks are `doc` type.
