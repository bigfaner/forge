---
status: "completed"
started: "2026-05-23 19:56"
completed: "2026-05-23 20:00"
time_spent: "~4m"
---

# Task Record: 2.7 Implement Run-to-Learn mechanism

## Summary
Implement Run-to-Learn mechanism: created R2L rule file for gen-test-scripts skill and added RuntimeCoverageRatio/ConfirmedRuntimeSubjects methods to facttable package for coverage calculation

## Changes

### Files Created
- plugins/forge/skills/gen-test-scripts/rules/run-to-learn.md

### Files Modified
- forge-cli/pkg/facttable/facttable.go
- forge-cli/pkg/facttable/facttable_test.go

### Key Decisions
- R2L implemented as a markdown rule file (run-to-learn.md) following Forge Plugin distribution model -- LLM follows rules at generation time, no Go code needed for the orchestration logic
- Added ConfirmedRuntimeSubjects and RuntimeCoverageRatio to facttable package to support the coverage formula: confirmed runtime facts covering Outcomes / total Outcomes * 100%
- Rule file placed at Step 1.5 in the gen-test-scripts pipeline (between Code Reconnaissance and Read Contract Specifications)

## Test Results
- **Tests Executed**: Yes
- **Passed**: 10
- **Failed**: 0
- **Coverage**: 96.0%

## Acceptance Criteria
- [x] R2L rule file defines skeleton test generation (setup + execution + output capture, no assertions)
- [x] R2L rule file defines running skeleton tests to capture stdout/stderr/exit code
- [x] R2L rule file defines writing runtime data to Fact Table (source: runtime, confidence: confirmed)
- [x] R2L rule file defines regeneration of more precise tests using enriched Fact Table
- [x] Exit condition: max 3 iterations or coverage >= 80%
- [x] Timeout protection (default 60s, configurable via .forge/config.yaml)
- [x] Failure handling: compilation failure records to Fact Table (kind: compilation_error)
- [x] Failure handling: runtime crash records to Fact Table (kind: runtime_crash)
- [x] Failure handling: dirty data output discards current round, preserves previous state
- [x] Failure handling: API write operations only GET, generates rollback statements
- [x] Fallback principle: R2L failure never blocks pipeline
- [x] R2L enabled via .forge/config.yaml run_to_learn: true or CLI flag
- [x] Runtime fact replaces same subject+kind static fact; non-confirmed runtime keeps static as fallback
- [x] Coverage calculation: confirmed/runtime facts covering Outcomes / total Outcomes * 100%
- [x] Skeleton tests run in temp directory, writes use isolated environment
- [x] Environment not ready skips R2L, uses static info

## Notes
R2L rule file is consumed by the LLM at test generation time. The Go code changes (RuntimeCoverageRatio, ConfirmedRuntimeSubjects) provide the coverage calculation primitive that the rule references. The facttable package now has 96% test coverage.
