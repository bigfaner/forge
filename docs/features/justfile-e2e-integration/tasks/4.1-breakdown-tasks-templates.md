---
id: "4.1"
title: "Update breakdown-tasks templates"
priority: "P0"
estimated_time: "1h"
dependencies: ["3.gate"]
status: pending
breaking: false
---

# 4.1: Update breakdown-tasks templates

## Description

Add explicit just commands to the Implementation Notes of three breakdown-tasks templates.

**run-e2e-tests.md changes:**
- Add to Implementation Notes: `Run: just test-e2e --feature <slug>` (replace `<slug>` with current feature name from `task feature`)

**gen-test-scripts.md changes:**
- Add to Implementation Notes: after generating spec files, run `just e2e-verify --feature <slug>`; if exit 1 (unresolved `// VERIFY:` markers), task is incomplete — resolve markers before proceeding to T-test-3

**fix-e2e.md changes:**
- Add post-fix verification step to Implementation Notes: after fixing, verify with `just test-e2e --feature <slug>`; all tests must pass before marking this task completed

## Reference Files

- `plugins/forge/skills/breakdown-tasks/templates/run-e2e-tests.md` — file to modify
- `plugins/forge/skills/breakdown-tasks/templates/gen-test-scripts.md` — file to modify
- `plugins/forge/skills/breakdown-tasks/templates/fix-e2e.md` — file to modify
- `docs/features/justfile-e2e-integration/design/tech-design.md` — Phase 4 changes

## Acceptance Criteria

- [ ] `grep -c 'just test-e2e' plugins/forge/skills/breakdown-tasks/templates/run-e2e-tests.md` >= 1
- [ ] `grep -c 'just e2e-verify' plugins/forge/skills/breakdown-tasks/templates/gen-test-scripts.md` >= 1
- [ ] `grep -c 'just test-e2e' plugins/forge/skills/breakdown-tasks/templates/fix-e2e.md` >= 1

## User Stories

Story 4 / AC-1: Given Agent has completed fix-e2e task, When Agent reads the Implementation Notes, Then `just test-e2e --feature <slug>` appears as the verification command.

## Implementation Notes

Make targeted additions only. Do not restructure the templates. Add the just commands as the last item in Implementation Notes for each template.
