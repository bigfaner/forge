---
id: "2.2"
title: "Update run-e2e-tests SKILL.md"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.gate"]
status: pending
breaking: false
---

# 2.2: Update run-e2e-tests SKILL.md

## Description

Replace all raw shell commands in `plugins/forge/skills/run-e2e-tests/SKILL.md` with just commands, covering both code blocks and prose descriptions.

Changes:
- Step 1 deps install (code block): `cd tests/e2e && npm install` → `just e2e-setup`
- Step 1 playwright install (code block): `npx playwright install chromium` → remove (covered by `just e2e-setup`)
- Step 1 verify install (code block): `npx playwright --version` → remove (covered by `just e2e-setup`)
- Step 1 prose: "Install dependencies..." / "Install Playwright browser..." → "Run `just e2e-setup` (idempotent — installs deps and Playwright browser):"
- Step 2 three spec commands (code blocks): `npx tsx <slug>/cli.spec.ts`, `npx tsx <slug>/api.spec.ts`, `npx tsx <slug>/ui.spec.ts` → `just test-e2e --feature <slug> 2>&1 | tee results/<slug>-output.txt`
- Error table: "Run `npx playwright install chromium`, retry" → "Run `just e2e-setup`, retry"
- Error table: "Run `npm install`, retry" → "Run `just e2e-setup`, retry"

## Reference Files

- `plugins/forge/skills/run-e2e-tests/SKILL.md` — file to modify
- `docs/features/justfile-e2e-integration/design/tech-design.md` — Phase 2 replacement table

## Acceptance Criteria

- [ ] `grep -c 'npx tsx\|npx playwright\|npm install' plugins/forge/skills/run-e2e-tests/SKILL.md` = 0
- [ ] `grep -c 'just e2e-setup\|just test-e2e' plugins/forge/skills/run-e2e-tests/SKILL.md` >= 2

## User Stories

Story 1 / AC-1: Given init-justfile has been run, When Skill maintainer views run-e2e-tests SKILL.md Step 1, Then only `just e2e-setup` appears, not raw npm/playwright commands.

## Implementation Notes

The three separate `npx tsx` spec commands collapse into one `just test-e2e --feature <slug>` call. The tee output path should be `results/<slug>-output.txt` (combined output for all spec types).
