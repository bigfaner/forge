---
id: "3.3"
title: "Update execute-task, record-task, and improve-harness"
priority: "P0"
estimated_time: "1h"
dependencies: ["2.gate"]
status: pending
breaking: false
---

# 3.3: Update execute-task, record-task, and improve-harness

## Description

Replace raw commands in three remaining files.

**execute-task.md changes (Step 3):**
- Prose: "Run project-specific verification commands." → "Run `just build && just test`."

**record-task SKILL.md changes (Metrics Collection):**
- Code block: replace three language examples (`go test -cover ./changed/package/...`, `npm test -- --coverage --watchAll=false`, `pytest --cov=<module> --cov-report=term-missing`) with single `just test`

**improve-harness SKILL.md changes (Step 4.3):**
- Prose: "Run project test suite to ensure nothing broke" → "Run `just test` to ensure nothing broke"

## Reference Files

- `plugins/forge/commands/execute-task.md` — file to modify
- `plugins/forge/skills/record-task/SKILL.md` — file to modify
- `plugins/forge/skills/improve-harness/SKILL.md` — file to modify
- `docs/features/justfile-e2e-integration/design/tech-design.md` — Phase 3 replacement table

## Acceptance Criteria

- [ ] `grep -c 'project-specific verification' plugins/forge/commands/execute-task.md` = 0
- [ ] `grep -c 'go test\|npm test.*coverage\|pytest --cov' plugins/forge/skills/record-task/SKILL.md` = 0
- [ ] `grep -c 'just test' plugins/forge/skills/record-task/SKILL.md` >= 1
- [ ] `grep -c 'just test' plugins/forge/skills/improve-harness/SKILL.md` >= 1

## User Stories

Story 5 / AC-3: Given Agent is executing record-task Metrics Collection, When Agent reads the example commands, Then `just test` appears, not `go test -cover ./...`, `npm test -- --coverage`, or `pytest --cov=...`.

## Implementation Notes

Make targeted edits only. For record-task, the three language examples collapse into one `just test` line. Keep the surrounding explanation text intact.
