---
status: "completed"
started: "2026-04-29 17:49"
completed: "2026-04-29 17:51"
time_spent: "~2m"
---

# Task Record: 3.1 Update fix-bug and run-tasks commands

## Summary
Replaced all raw test/build commands in fix-bug.md and run-tasks.md with just test and just test-e2e targets. Step 2 baseline now uses just test, Step 3a uses just test, Step 3b uses just test-e2e --feature <slug>, Step 5 uses just build && just test plus just test-e2e --feature <slug>. run-tasks.md Step 5 breaking gate now uses just test.

## Changes

### Files Created
无

### Files Modified
- plugins/forge/commands/fix-bug.md
- plugins/forge/commands/run-tasks.md

### Key Decisions
- Step 3a prose updated to 'Run just test — it must fail before the fix' to match task spec exactly
- Step 5 prose updated to 'Run just build && just test. All must pass.' per task spec
- run-tasks.md Step 5 comment replaced with just test, removing go test/npm test/pytest references

## Test Results
- **Passed**: 0
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] grep -c 'project-test-command\|npx tsx' plugins/forge/commands/fix-bug.md = 0
- [x] grep -c 'go test\|npm test\|pytest' plugins/forge/commands/run-tasks.md = 0
- [x] grep -c 'just test\|just test-e2e' plugins/forge/commands/fix-bug.md >= 3

## Notes
无
