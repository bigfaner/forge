---
id: "3.2"
title: "Update task-executor and error-fixer agents"
priority: "P0"
estimated_time: "1h"
dependencies: ["2.gate"]
status: pending
breaking: false
---

# 3.2: Update task-executor and error-fixer agents

## Description

Replace language-specific verification commands in `plugins/forge/agents/task-executor.md` and `plugins/forge/agents/error-fixer.md`.

**task-executor.md changes (Step 3):**
- Code block: replace three language examples (`go build ./... && go vet ./... && go test -race -cover ./...`, `npm run build && npm test`, `pytest --cov`) with single `just build && just test`
- Prose: "Run complete verification suite for your project:" → "Run `just build && just test`:"

**error-fixer.md changes (Step 4):**
- Same three language examples → `just build && just test`
- Prose: "Run complete verification suite for your project:" → "Run `just build && just test`:"

## Reference Files

- `plugins/forge/agents/task-executor.md` — file to modify
- `plugins/forge/agents/error-fixer.md` — file to modify
- `docs/features/justfile-e2e-integration/design/tech-design.md` — Phase 3 replacement table

## Acceptance Criteria

- [ ] `grep -c 'go test\|npm test\|pytest\|npm run build' plugins/forge/agents/task-executor.md` = 0
- [ ] `grep -c 'go test\|npm test\|pytest\|npm run build' plugins/forge/agents/error-fixer.md` = 0
- [ ] `grep -c 'just build && just test' plugins/forge/agents/task-executor.md` >= 1
- [ ] `grep -c 'just build && just test' plugins/forge/agents/error-fixer.md` >= 1

## User Stories

Story 2 / AC-1: Given Agent is executing task-executor Step 3 Full Verification, When Agent reads the instruction, Then `just build && just test` appears, not language-specific commands.

## Implementation Notes

Make targeted edits only. The language examples in the code block comments should be replaced entirely — do not keep them as secondary examples.
