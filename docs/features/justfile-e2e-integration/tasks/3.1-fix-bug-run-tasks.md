---
id: "3.1"
title: "Update fix-bug and run-tasks commands"
priority: "P0"
estimated_time: "1h"
dependencies: ["2.gate"]
status: pending
breaking: false
---

# 3.1: Update fix-bug and run-tasks commands

## Description

Replace all raw test/build commands in `plugins/forge/commands/fix-bug.md` and `plugins/forge/commands/run-tasks.md`.

**fix-bug.md changes:**
- Step 2 code block: `<project-test-command>   # e.g. npm test, go test ./..., pytest` → `just test`
- Step 2 prose: "Run existing tests to establish baseline" → "Run `just test` to establish baseline"
- Step 3a code block: `<project-test-command> --testNamePattern "bug:"` → `just test`
- Step 3a prose: "Run the new test — it must fail before the fix:" → "Run `just test` — it must fail before the fix:"
- Step 3b code block: `npx tsx <spec-file> 2>&1` → `just test-e2e --feature <slug>`
- Step 3b prose: "Run the e2e test — it must fail before the fix:" → "Run `just test-e2e --feature <slug>` — it must fail before the fix:"
- Step 5 code block: `<project-test-command>` → `just test`
- Step 5 code block: `npx tsx <spec-file> 2>&1` → `just test-e2e --feature <slug>`
- Step 5 prose: "Run the full test suite. All tests must pass." → "Run `just build && just test`. All must pass."

**run-tasks.md changes:**
- Step 5 code block comment: `# go test ./... OR npm test OR the testCommand from index.json` → `just test`
- Step 5 prose: "Run project-level full test suite" → "Run `just test`"

## Reference Files

- `plugins/forge/commands/fix-bug.md` — file to modify
- `plugins/forge/commands/run-tasks.md` — file to modify
- `docs/features/justfile-e2e-integration/design/tech-design.md` — Phase 3 replacement table

## Acceptance Criteria

- [ ] `grep -c 'project-test-command\|npx tsx' plugins/forge/commands/fix-bug.md` = 0
- [ ] `grep -c 'go test\|npm test\|pytest' plugins/forge/commands/run-tasks.md` = 0
- [ ] `grep -c 'just test\|just test-e2e' plugins/forge/commands/fix-bug.md` >= 3

## User Stories

Story 5 / AC-1: Given Agent is executing fix-bug command, When Agent reads the test verification steps, Then only `just test` appears, not `<project-test-command>` or language-specific commands.
Story 5 / AC-2: Given Agent is executing run-tasks Breaking Gate, When Agent reads the gate instruction, Then `just test` appears, not `npm test` or `go test`.

## Implementation Notes

Make targeted edits only. Do not restructure the files.
