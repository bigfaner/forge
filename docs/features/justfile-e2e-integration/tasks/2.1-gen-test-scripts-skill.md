---
id: "2.1"
title: "Update gen-test-scripts SKILL.md"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.gate"]
status: pending
breaking: false
---

# 2.1: Update gen-test-scripts SKILL.md

## Description

Replace all raw shell commands in `plugins/forge/skills/gen-test-scripts/SKILL.md` with just commands, covering both code blocks and prose descriptions.

Changes:
- Step 4 post-generation check (code block): `grep -r '// VERIFY:' tests/e2e/<feature>/` → `just e2e-verify --feature <slug>`
- Step 4 post-generation check (prose): update description to reference `just e2e-verify --feature <slug>` and add explicit note: "exit 1 = skill incomplete, do not proceed to run-e2e-tests"
- Step 5 deps install (code block): `cd tests/e2e && npm install` → `just e2e-setup`

## Reference Files

- `plugins/forge/skills/gen-test-scripts/SKILL.md` — file to modify
- `docs/features/justfile-e2e-integration/design/tech-design.md` — Phase 2 replacement table

## Acceptance Criteria

- [ ] `grep -c 'npx playwright install\|cd tests/e2e' plugins/forge/skills/gen-test-scripts/SKILL.md` = 0
- [ ] `grep -c 'just e2e-verify\|just e2e-setup' plugins/forge/skills/gen-test-scripts/SKILL.md` >= 2
- [ ] Step 4 prose contains "exit 1 = skill incomplete" note

## User Stories

Story 3 / AC-1: Given Agent has generated spec files, When Agent runs `just e2e-verify --feature <slug>` and markers remain, Then exit 1 and skill is marked incomplete.

## Implementation Notes

Make targeted edits only. Do not restructure the SKILL.md. The hard gate note must be explicit: agent must not call run-e2e-tests if exit code is 1.
