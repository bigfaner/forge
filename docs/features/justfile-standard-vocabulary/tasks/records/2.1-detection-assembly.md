---
status: "completed"
started: "2026-04-30 01:44"
completed: "2026-04-30 01:49"
time_spent: "~5m"
---

# Task Record: 2.1 init-justfile project-type detection + template assembly

## Summary
Implemented project-type detection and template assembly logic in init-justfile.md. Added Model 4 ProjectDetection: checks package.json (frontend), go.mod/Cargo.toml/pyproject.toml (backend), classifies as mixed/frontend/backend/error. Added boundary marker merge for idempotent re-runs. Added --force flag for agent workflows. Added interactive confirmation for human users. Added template selection table mapping project type to correct template section. Created 19 e2e tests covering all acceptance criteria.

## Changes

### Files Created
- tests/e2e/justfile-e2e-integration/detection-assembly.spec.ts

### Files Modified
- plugins/forge/commands/init-justfile.md

### Key Decisions
- Detection uses 4 marker files: package.json (frontend), go.mod/Cargo.toml/pyproject.toml (backend)
- Classification algorithm: both signals=mixed, frontend only=frontend, backend only=backend, neither=error with abort
- Boundary marker merge: when forge markers exist, only replaces marked section preserving custom recipes
- --force flag skips all interactive prompts for agent workflows (non-interactive mode)
- Interactive confirmation only triggers when justfile exists without forge markers and --force is not provided
- Template selection uses table lookup: frontend->Frontend Template, backend->Backend Template, mixed->Mixed Template
- Step numbering restructured: Step 1=Detect, Step 2=Check Existing, Step 3=Assemble and Write

## Test Results
- **Passed**: 42
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] Detection logic checks for package.json (frontend signal), go.mod/Cargo.toml/pyproject.toml (backend signals)
- [x] Classification: both signals -> mixed, frontend only -> frontend, backend only -> backend, neither -> error message
- [x] Generates correct project-type recipe variant for each project type
- [x] Selects and assembles the matching template (backend/frontend/mixed) from tasks 1.2-1.4
- [x] Boundary marker merge: when justfile exists with markers, replaces only the marked section
- [x] --force flag support: skips user confirmation for agent use (non-interactive)
- [x] Interactive confirmation: when no boundary markers exist and justfile exists, prompts user before overwriting
- [x] Generated justfile wrapped in boundary markers
- [x] Error message when no known project markers detected

## Notes
无
